# فحص النظام العميق — سجل الملاحظات والأخطاء — 2026-06-20

- **التوجيه:** «راجع وتأكد من حل السابق، ثم افحص النظام بعمق، جرّب، طلّع أخطاء ومشاكل، قيّدها، ثم أصلحها من جذورها وعلى نفس سياقها.»
- **الأساس:** HEAD = `5cc0a31` (الجلسة السابقة، guard.sh أخضر). بيئة بلا قاعدة بيانات (DATABASE_URL=mysql غير موصول) → فحوص/اختبارات DB تتخطّى عبر `env -u DATABASE_URL`.
- **المنهج:** التحقق من إصلاحات الجلسة السابقة (9 ملفات اختبار، 44 ✓) → مسابير ثابتة للأنماط الخطرة → قراءة نقدية للميزات الجديدة (الأحدث، الأقل اختبارًا) → إصلاح آمن دفعةً دفعةً عبر guard.sh.

## الحالة العامة
- ✅ إصلاحات الجلسة السابقة سليمة: endpointCoverage/auditIgoc/scopeHelper/resourceTableNames + smoke = 44 ✓.
- ✅ الانضباط غير المتزامن نظيف: مسبار «floating writes بلا .catch» = صفر إيجابيات حقيقية (الكل fire-and-forget بـ .catch، أو ضمن Promise.all/await).

## الملاحظات (مُقيَّدة)

### F1 — [تشغيلي/آمن] PATCH الموعد لا يتحقق من ترتيب البداية/النهاية
- **الموقع:** `artifacts/api-server/src/routes/calendar.ts` — PATCH `/appointments/:id`.
- **العيب:** POST يرفض `endsAt < startsAt`، لكن PATCH الجزئي لا يتحقق — يمكن تعديل `startsAt` لما بعد `endsAt` (أو العكس) فينتج موعد غير صالح.
- **الإصلاح (الجذر، نفس السياق):** عند تغيير `startsAt` و/أو `endsAt`، تُحمَّل القيم الحالية، تُحسب القيمة الفعّالة، ويُرفض الترتيب المقلوب — كمرآة لتحقّق POST.
- **التحقق:** تأكيد ثابت في `appointmentsSmoke.test.ts` + tsc + guard.
- **الحالة:** [x] مُصلَح.

### F2 — [مالي-مجاور/آمن: تحقق إدخال فقط، لا تغيير ترحيل] النقد في الطريق لا يتحقق من تمايز حساب المقاصّة
- **الموقع:** `artifacts/api-server/src/routes/finance-cash-in-transit.ts` — POST `/cash-in-transit`.
- **العيب:** يُرفض `source === destination` فقط. لو `clearing === destination` فالمال «يصل» عند الإرسال (قيد التأكيد يصبح غسلًا) — يُفرِغ معنى «في الطريق». ولو `clearing === source` فقيد الإرسال غسل بلا أثر.
- **الإصلاح (الجذر، نفس السياق):** حارس إدخال إضافي يرفض `clearing === source || clearing === destination` — لا يمسّ منطق الترحيل ولا الحسابات ولا المبالغ (مثل حارس `source !== destination` القائم).
- **التحقق:** تأكيد ثابت في `cashInTransitSmoke.test.ts` + tsc + guard.
- **الحالة:** [x] مُصلَح.

### F3 — [يمسّ الدفتر → تصعيد لإبراهيم، لا إصلاح ذاتي] التشغيل اليدوي للفاتورة المتكررة يستهلك الدورة التالية
- **الموقع:** `artifacts/api-server/src/lib/recurringInvoiceProcessor.ts` — `runRecurringInvoice` مع `force=true`.
- **العيب:** عند تشغيل يدوي قبل الاستحقاق، يُبنى مفتاح الـidempotency على `nextRunDate` المستقبلي ويُقدَّم `nextRunDate` — فالفاتورة المجدوَلة لاحقًا لنفس التاريخ تُحجَب (نفس sourceKey) وتُستهلك الدورة. الفاتورة مؤرّخة باليوم بينما مفتاحها بتاريخ مستقبلي.
- **لماذا لا أصلحه ذاتيًّا:** التوليد يمسّ الدفتر عبر `financialEngine.postSalesInvoice` + تغيير منطق الـidempotency/الجدولة قرار محاسبي. الدستور: يتطلّب اعتماد إبراهيم + assertion tests على سطور القيد.
- **الاقتراح (لإبراهيم):** فصل التشغيل اليدوي «الفوري» (مفتاح مؤرّخ باليوم، لا يقدّم nextRunDate) عن «التبكير المجدوَل» (يقدّم). قرار نعم/لا.
- **الحالة:** [ ] مُصعَّد — بانتظار قرار إبراهيم.

### F4 — [تشغيلي/آمن] PATCH تعريف الحقل لا يتحقق من «select يتطلب خيارات»
- **الموقع:** `artifacts/api-server/src/routes/customFields.ts` — PATCH `/definitions/:id`.
- **العيب:** POST يرفض `fieldType==="select"` بلا options، لكن PATCH لا يتحقق — يمكن تحويل النوع إلى select بلا خيارات فينتج حقل قائمة معطوب (قائمة منسدلة فارغة).
- **الإصلاح (الجذر، نفس السياق):** عند تغيير `fieldType` و/أو `options` تُحمَّل القيم الحالية، تُحسب القيمة الفعّالة، ويُرفض `select` الفعّال بلا خيارات — كمرآة لتحقّق POST.
- **التحقق:** تأكيد ثابت في `customFieldsSmoke.test.ts` + tsc + guard.
- **الحالة:** [x] مُصلَح.

> **نمط متكرّر مُلاحَظ:** «POST يتحقق، وPATCH الجزئي لا» (F1، F4). عند إضافة أي PATCH
> جزئي، أعد تطبيق تحقّقات POST على القيمة الفعّالة (الواردة أو المخزَّنة).

## الدفعات
- **B1 (تشغيلي، آمن):** F1 + F2 + F4 + تأكيداتها الثابتة → عبر guard.sh ثم push. (هذه الدفعة)
- **F3:** تقرير فقط — لا تطبيق بلا اعتماد إبراهيم (يمسّ الدفتر).
