# فحص النظام العميق — سجل الملاحظات والأخطاء — 2026-06-20

- **التوجيه:** «راجع وتأكد من حل السابق، ثم افحص النظام بعمق، جرّب، طلّع أخطاء ومشاكل، قيّدها، ثم أصلحها من جذورها وعلى نفس سياقها.»
- **الأساس:** HEAD = `5cc0a31` (الجلسة السابقة، guard.sh أخضر). بيئة بلا قاعدة بيانات (DATABASE_URL=mysql غير موصول) → فحوص/اختبارات DB تتخطّى عبر `env -u DATABASE_URL`.
- **المنهج:** التحقق من إصلاحات الجلسة السابقة (9 ملفات اختبار، 44 ✓) → مسابير ثابتة للأنماط الخطرة → قراءة نقدية للميزات الجديدة (الأحدث، الأقل اختبارًا) → إصلاح آمن دفعةً دفعةً عبر guard.sh.

## الحالة العامة
- ✅ إصلاحات الجلسة السابقة سليمة: endpointCoverage/auditIgoc/scopeHelper/resourceTableNames + smoke = 44 ✓.
- ✅ الانضباط غير المتزامن نظيف: مسبار «floating writes بلا .catch» = صفر إيجابيات حقيقية (الكل fire-and-forget بـ .catch، أو ضمن Promise.all/await).

## الملاحظات (مُقيَّدة)

### F1 — [تشغيلي/آمن] PATCH الموعد لا يتحقق من ترتيب البداية/النهاية
- **الموقع:** `artifacts/api-server/src/routes/calendar.ts` — PATCH `/appointments/:id`.
- **العيب:** POST يرفض `endsAt < startsAt`، لكن PATCH الجزئي لا يتحقق — يمكن تعديل `startsAt` لما بعد `endsAt` (أو العكس) فينتج موعد غير صالح.
- **الإصلاح (الجذر، نفس السياق):** عند تغيير `startsAt` و/أو `endsAt`، تُحمَّل القيم الحالية، تُحسب القيمة الفعّالة، ويُرفض الترتيب المقلوب — كمرآة لتحقّق POST.
- **التحقق:** تأكيد ثابت في `appointmentsSmoke.test.ts` + tsc + guard.
- **الحالة:** [x] مُصلَح.

### F2 — [مالي-مجاور/آمن: تحقق إدخال فقط، لا تغيير ترحيل] النقد في الطريق لا يتحقق من تمايز حساب المقاصّة
- **الموقع:** `artifacts/api-server/src/routes/finance-cash-in-transit.ts` — POST `/cash-in-transit`.
- **العيب:** يُرفض `source === destination` فقط. لو `clearing === destination` فالمال «يصل» عند الإرسال (قيد التأكيد يصبح غسلًا) — يُفرِغ معنى «في الطريق». ولو `clearing === source` فقيد الإرسال غسل بلا أثر.
- **الإصلاح (الجذر، نفس السياق):** حارس إدخال إضافي يرفض `clearing === source || clearing === destination` — لا يمسّ منطق الترحيل ولا الحسابات ولا المبالغ (مثل حارس `source !== destination` القائم).
- **التحقق:** تأكيد ثابت في `cashInTransitSmoke.test.ts` + tsc + guard.
- **الحالة:** [x] مُصلَح.

### F3 — [يمسّ الدفتر → تصعيد لإبراهيم، لا إصلاح ذاتي] التشغيل اليدوي للفاتورة المتكررة يستهلك الدورة التالية
- **الموقع:** `artifacts/api-server/src/lib/recurringInvoiceProcessor.ts` — `runRecurringInvoice` مع `force=true`.
- **العيب:** عند تشغيل يدوي قبل الاستحقاق، يُبنى مفتاح الـidempotency على `nextRunDate` المستقبلي ويُقدَّم `nextRunDate` — فالفاتورة المجدوَلة لاحقًا لنفس التاريخ تُحجَب (نفس sourceKey) وتُستهلك الدورة. الفاتورة مؤرّخة باليوم بينما مفتاحها بتاريخ مستقبلي.
- **لماذا لا أصلحه ذاتيًّا:** التوليد يمسّ الدفتر عبر `financialEngine.postSalesInvoice` + تغيير منطق الـidempotency/الجدولة قرار محاسبي. الدستور: يتطلّب اعتماد إبراهيم + assertion tests على سطور القيد.
- **الاقتراح (لإبراهيم):** فصل التشغيل اليدوي «الفوري» (مفتاح مؤرّخ باليوم، لا يقدّم nextRunDate) عن «التبكير المجدوَل» (يقدّم). قرار نعم/لا.
- **الحالة:** [ ] مُصعَّد — بانتظار قرار إبراهيم.

### F4 — [تشغيلي/آمن] PATCH تعريف الحقل لا يتحقق من «select يتطلب خيارات»
- **الموقع:** `artifacts/api-server/src/routes/customFields.ts` — PATCH `/definitions/:id`.
- **العيب:** POST يرفض `fieldType==="select"` بلا options، لكن PATCH لا يتحقق — يمكن تحويل النوع إلى select بلا خيارات فينتج حقل قائمة معطوب (قائمة منسدلة فارغة).
- **الإصلاح (الجذر، نفس السياق):** عند تغيير `fieldType` و/أو `options` تُحمَّل القيم الحالية، تُحسب القيمة الفعّالة، ويُرفض `select` الفعّال بلا خيارات — كمرآة لتحقّق POST.
- **التحقق:** تأكيد ثابت في `customFieldsSmoke.test.ts` + tsc + guard.
- **الحالة:** [x] مُصلَح.

> **نمط متكرّر مُلاحَظ:** «POST يتحقق، وPATCH الجزئي لا» (F1، F4، F5). عند إضافة أي
> PATCH جزئي، أعد تطبيق تحقّقات POST على القيمة الفعّالة (الواردة أو المخزَّنة).

### F5 — [تشغيلي/آمن] PATCH برنامج التدريب لا يتحقق من ترتيب البداية/النهاية
- **الموقع:** `artifacts/api-server/src/routes/training.ts` — PATCH `/programs/:id`.
- **العيب:** POST `/programs` يرفض `endDate < startDate`، لكن PATCH يحدّث `startDate`/`endDate` بلا إعادة تحقّق.
- **الإصلاح (الجذر، نفس السياق):** توسيع SELECT الحالي ليجلب التاريخين + تحقّق القيمة الفعّالة كمرآة لـ POST.
- **التحقق:** اختبار جديد `trainingProgramDateOrderingSmoke.test.ts` + tsc + guard.
- **الحالة:** [x] مُصلَح.

### مسح منهجي للنمط (الجولة 3) — تواريخ النطاق في التحديث
فُحصت كل ملفات المسارات ذات تحقّق ترتيب التواريخ عند الإنشاء:
- ✅ `properties.ts` PATCH `/contracts/:id` — **سليم أصلًا** (يحسب القيمة الفعّالة ويرفض `ed <= sd`؛ نفس نمط الإصلاح).
- ✅ `hr.ts` shift-assignments — **لا تحديث** (POST فقط) → لا ثغرة.
- ⚠️ `training.ts` — **ثغرة F5** (أُصلحت).
- بقية الملفات: انظر «الجولة 4» أدناه (تعميم كامل).

### الجولة 4 — تعميم «التحديث يطابق تحقّق الإنشاء» على كل المسارات (تصرّف بالأقوى)
مسح شامل لكل معالج PATCH/PUT يكتب زوج نطاق تاريخي، مقابل تحقّق إنشائه:
- ✅ **F6 (مُصلَح):** `marketing.ts` PATCH `/campaigns/:id` — الإنشاء يرفض `endDate<startDate` (س118)، التحديث لا. أُضيف تحقّق القيمة الفعّالة.
- ✅ **F7 (مُصلَح):** `tasks.ts` PATCH `/:id` — الإنشاء يرفض `scheduledEnd<scheduledStart` (س452)، التحديث لا. أُضيف تحقّق القيمة الفعّالة (يحمّل المخزَّن).
- ✅ سليمة أصلًا: `properties.ts` (عقود)، `fleet.ts` (س3837)، `calendar.ts`/`customFields.ts`/`training.ts` (F1/F4/F5).
- ⏭️ **مُستثنى بحق:** `hr.ts` startTime/endTime للورديات — وقت اليوم قد يلتفّ منتصف الليل (وردية ليلية)، فـ end<start مشروع → لا يُضاف تحقّق.
- 🟡 **يُسجَّل (لا يُطبَّق ذاتيًّا — يؤسّس ثابتًا جديدًا في مسار حسّاس):** `umrah.ts` `/seasons` و`hr.ts` `/public-holidays` — **لا الإنشاء ولا التحديث** يتحقق من الترتيب. إضافة ثابت جديد لمسار قائد (العمرة) قرار يُعرَض، لا يُفرَض ضمن دفعة إصلاح. توصية: مسح حوكمة مخصّص يضيف التحقّق للإنشاء+التحديث معًا.

### مسابير سلامة أخرى (الجولة 2) — كلها نظيفة
- الانضباط غير المتزامن (fire-and-forget بـ .catch): نظيف.
- إساءة `rawQuery().rows` (rawQuery يُعيد مصفوفة): صفر.
- `JSON.parse` على أعمدة jsonb: كلها محميّة بـ `typeof === "string"` + try/catch.
- N+1 في الملفات الجديدة: فقط حلقة upsert محدودة في customFields (مقبولة).
- حقن iCalendar (.ics): `icsEscape` يهرّب `\ ; , \n` بالترتيب الصحيح + بريد الحضور مُتحقَّق `z.email()` → آمن.

## الدفعات
- **B1 (تشغيلي، آمن):** F1 + F2 + F4 + تأكيداتها الثابتة → عبر guard.sh ثم push. (مُنفَّذة)
- **F3:** تقرير فقط — لا تطبيق بلا اعتماد إبراهيم (يمسّ الدفتر).

## جولة 5 — حفر أوسع بعد سؤال إبراهيم «معقولة مافيه أخطاء؟» (محقّ)
الاعتراف: حكم «لا ملاحظات» كان مقصورًا على diff جلستي (نظيف فعلًا)، لا على النظام.

### F8 — [تشغيلي/آمن، يخالف الدستور «لا تخفِ فشل»] ابتلاع الخطأ الصامت في 20 نقطة GET
- **الموقع:** `hr.ts` (18) + `employees.ts` (2) — `catch { logger.error(...); res.json({data:[],total:0}) }`.
- **العيب:** عند فشل الاستعلام تُعاد قائمة فارغة/أصفار بحالة **200** → المستخدم يرى «لا بيانات» بدل خطأ؛ والمدير قد يرى «0 مخالفات» بينما الاستعلام فشل (مضلّل). نفس صنف #2301 (الفشل الصامت).
- **الإصلاح (الجذر، نفس السياق):** محاذاة للنمط القياسي `handleRouteError(err, res, "...")` المستعمل في مئات النقاط الأخرى بنفس الملفين. مسار النجاح بلا تغيير؛ مسار الفشل فقط (200+فارغ → حالة خطأ). الواجهة تعالج الخطأ لكل widget مستقلًّا (useApiQuery + PageStateWrapper) فلا تتالي انهيار.
- **التحقق:** صفر نمط متبقٍّ + tsc نظيف + guard.sh الكامل (شبكة أمان 13193 اختبارًا).
- **الحالة:** [x] مُصلَح.

### F9 — [يمسّ الدفتر → يُسجَّل لا يُطبَّق] حقول مبالغ بلا `.positive()/.min()`
- **المواقع:** `finance-recurring.ts:73-74` (debit/credit في سطور القيد، `default(0)` بلا حدّ)، `properties.ts` (paidAmount/amount لدفعات الإيجار)، `umrah.ts:445`.
- **العيب المحتمل:** قبول مبلغ/مدين/دائن سالب. لكن بعض الحقول قد تكون عكسية مشروعة (إشعار دائن/تسوية) — لذا لا أُعمّم `.positive()` بلا مراجعة دلالية لكل حقل، وهي تغذّي الترحيل → بوابة الدفتر.
- **الحالة:** [ ] مُصعَّد — مراجعة دلالية + اعتماد إبراهيم.

### مسابير نظيفة (لا خطأ)
- القوائم غير المحدودة: كل ما ظهر **محدود أصلًا** (سطور كيان واحد/صف إعدادات/فروع المستأجر) — لا قائمة مفتوحة.
- علامات الكود الناقص: 7 TODO فقط، صفر `@ts-ignore` — انضباط جيد.
