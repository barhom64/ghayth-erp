# خارطة طريق: النظام الاستباقي وسدّ الفجوات البديهية — نظام غيث

- **التاريخ:** 2026-06-20
- **التوجيه:** «قيّدها وجدولها — افحص أولًا نظامنا، بعدها ننفّذها» (إبراهيم).
- **الوضع:** **تخطيط وقرار فقط.** لا تنفيذ في هذه الدفعة — التنفيذ مؤجّل صراحةً («بعدها ننفّذها»).
- **المرجع:** `docs/ghayth-constitution.md` + الفهرس الجامع #2710.
- **المنهجية:** كل بند أدناه **مدقّق فعليًا على الكود** (ملف:سطر)، لا تخمين. ما ظهر أنه موجود فعلًا **شُطب من النواقص وصُحِّح**.

---

## 1) نتيجة التدقيق — تصحيح المسلّمات قبل الجدولة

التدقيق الآلي بالغ في الإبلاغ عن «نواقص». التحقق اليدوي المباشر صحّح ما يلي. **هذه البنود موجودة فعلًا ولا تُجدول كنواقص:**

| القدرة | الحكم الفعلي | الدليل |
|---|---|---|
| تحويل بين الخزائن/الحسابات | **موجود فعلًا** — معالج كامل DR/CR + رسوم اختيارية | `pages/create/finance/account-transfer.tsx` |
| خطابات الضمان (LG) | **موجود فعلًا** — CRUD + دورة حياة (إلغاء/تحرير) + تنبيه انتهاء | `pages/finance/bank-guarantees.tsx` |
| تحويل مخزون بين المستودعات | **موجود فعلًا** — آلة حالة transfer_in/out + ترقيم | `warehouse.ts:35` · `movements-create.tsx` |
| فحص الجودة عند الاستلام (QC) | **موجود فعلًا** — اعتماد/رفض/استدعاء الدفعة بنقاط نهاية مخصّصة | `pages/warehouse/lots.tsx:62-109` (`qc-approve` / `qc-reject` / `recall`) |
| حماية الراتب (WPS / مدد) | **موجود فعلًا** — توليد ملف + إرسال + استلام تأكيد البنك | `routes/hr-wps.ts` |
| منع تكرار **العميل** | **موجود فعلًا** — فحص بريد + هاتف مع `FOR UPDATE` ورفض ConflictError | `routes/clients.ts:215-229` |
| القيود المتكررة (recurring journals) | **موجود فعلًا** (قيود لا فواتير) | `routes/finance-recurring.ts` |

> **درس حوكمي:** التدقيق الآلي وحده لا يكفي لإعلان «ناقص». كل بند معماري/يمس الدفتر يُتحقَّق منه يدويًا قبل الجدولة. صُحِّح بندان كانا سيُجدولان خطأً (#2716 و #2714) — انظر القسم 3.

---

## 2) النواقص المؤكَّدة (مدقَّقة) — مجدولة للتنفيذ لاحقًا

| البند | الحكم | الدليل | التصنيف الحوكمي | التذكرة |
|---|---|---|---|---|
| **أمان**: 2FA + إدارة الجلسات + تنبيه دخول | ناقص (الجلسات قائمة، 2FA غائب) | `auth.ts` (جلسات فقط) | معماري (جداول/أسرار TOTP) | #2712 |
| **سلامة بيانات**: قفل التعارض المتزامن (optimistic locking) | ناقص (قفل الفترات فقط) | `207_financial_periods_lock_columns.sql` | معماري (عمود rowVersion عام) | #2718 |
| **سلامة بيانات**: سلة المحذوفات + استرجاع | ناقص (الـschema جاهز، لا UI) | soft-delete موجود؛ لا شاشة استرجاع | تشغيلي (سطح واسع) | #2713 |
| **منع تكرار المورد** + رقم ضريبي/سجل تجاري | ناقص للمورد (INSERT مباشر بلا فحص) | `finance-vendors.ts:124-132` | تشغيلي (يحاكي نمط العميل) | #2716 (مصحّح) |
| **الدفتر**: اختيار الخزنة في سند القبض | ناقص | `customer-receipt.tsx` (لا مُحدِّد خزنة) | **يمس الدفتر** | #2698 |
| **الدفتر**: نقد في الطريق + خزنة سند الصرف | ناقص (التحويل نفسه موجود) | — | **يمس الدفتر** | #2714 (مصحّح) |
| **الدفتر**: الفوترة المتكررة للعملاء (اشتراكات) | ناقص (قيود متكررة فقط، لا فواتير) | `finance-recurring.ts` (قوالب GL) | **يمس الدفتر** | **جديد** |
| إغلاق الحلقة: تأكيد إنجاز + محضر→بنود + تفويض اعتماد | ناقص | — | تشغيلي/سير عمل | #2707 |
| توقيع العميل على الاستلام (POD) | ناقص | — | تشغيلي | #2715 |
| جدولة اجتماعات/مواعيد + دعوة `.ics` | ناقص | `calendar.ts` للقراءة فقط | تشغيلي | #2704 |
| مهام متكررة + توليد مهام تلقائي من الأحداث | ناقص | — | تشغيلي | #2705 |
| التقويم الحيّ: إنشاء + حجز موارد + مزامنة + ورديات | ناقص | — | تشغيلي/معماري | #2706 |
| قنوات ثنائية الاتجاه: واتساب/SMS + بريد وارد→تذكرة + digest | ناقص (SMS/واتساب stubs) | `integrationService.ts:210-215` | معماري (تكامل) | #2708 |
| ذكاء استباقي: إجراءات جماعية + تنبؤ | ناقص | — | تشغيلي | #2709 |
| حالة الفاتورة عند العميل + تذكير دفع تلقائي | ناقص | لا تتبّع «شوهدت» | تشغيلي | #2720 |
| **استيراد كشف بنكي / host-to-host** (MT940/OFX) | ناقص (خوارزمية المطابقة موجودة، لا استيعاب) | `finance-algorithms.ts` (مطابقة فقط) | معماري (تكامل بنكي) | **جديد** |
| بوابة دفع إلكتروني (مدى/Moyasar/HyperPay) | جزئي (SADAD موجود، لا بطاقة B2C) | `lib/saudiIntegrations/sadadClient.ts` | معماري + يمس الدفتر | #2717 |
| حقول مخصّصة لكل شركة | ناقص (أعمدة ثابتة) | `186_hr_form_field_persistence.sql` | معماري | #2719 |
| ساعات العمل (timesheet) + الفوترة بالوقت | ناقص (حضور/إضافي فقط) | `hr/tracking-policies` | تشغيلي/يمس الدفتر | #2721 |
| **RFQ → مقارنة عروض → أمر شراء** | ناقص (أمر شراء مباشر فقط) | لا نقاط RFQ في `finance-purchase.ts` | تشغيلي/معماري | **جديد** |
| **نقطة بيع (POS)** | ناقص (طباعة حرارية فقط) | `lib/print/*` فقط | معماري | **جديد** |
| **تكامل حكومي حيّ**: قوى + مزامنة مقيم/تم/أبشر فعلية | ناقص/سقالة (مدد فقط حقيقي) | `gov-integrations.ts:284-299` (فحص DNS فقط) | معماري (تكامل) | **جديد** |

---

## 3) تصحيحات لازمة على تذاكر قائمة

- **#2716 (منع التكرار):** يُضيّق النطاق — **العميل محمي فعلًا** (بريد+هاتف، `FOR UPDATE`). الناقص = **منع تكرار المورد** + فحص **الرقم الضريبي/السجل التجاري** للطرفين.
- **#2714 (حركة الخزائن):** يُضيّق النطاق — **التحويل بين الخزائن موجود فعلًا** (`account-transfer.tsx`). الناقص = **النقد في الطريق** + **اختيار خزنة سند الصرف/القبض** (يلتقي مع #2698).

---

## 4) الجدول الزمني بالمراحل (مرتّب: أمان وسلامة أولًا، ثم الدفتر، ثم الأتمتة)

| الدفعة | المحتوى | الأولوية | التصنيف | البوابة قبل «تم» |
|---|---|---|---|---|
| **P0 — أمان وسلامة البيانات** | منع تكرار المورد + رقم ضريبي (#2716) · سلة المحذوفات/استرجاع (#2713) | عالية | تشغيلي غالبًا | المجلس + سكربتات التدقيق |
| **P0+ — أمان معماري** | 2FA + إدارة جلسات (#2712) · قفل التعارض المتزامن (#2718) | عالية | **معماري** | **قرار إبراهيم + هجرة + مجلس** |
| **P1 — بديهيات الدفتر (إغلاق الحلقة المالية)** | خزنة سند القبض (#2698) · نقد في الطريق + خزنة الصرف (#2714) · الفوترة المتكررة للعملاء (جديد) | عالية | **يمس الدفتر** | **قرار إبراهيم + assertion tests + مجلس** |
| **P1+ — إغلاق حلقة العمل** | تأكيد الإنجاز/المحضر/التفويض (#2707) · توقيع الاستلام POD (#2715) | متوسطة | تشغيلي | المجلس |
| **P2 — الجدولة والأتمتة الاستباقية** | مواعيد + `.ics` (#2704) · مهام متكررة (#2705) · التقويم الحيّ (#2706) · قنوات ثنائية (#2708) · ذكاء استباقي (#2709) · تذكير دفع + حالة الفاتورة (#2720) · استيراد كشف بنكي (جديد) | متوسطة | تشغيلي/معماري | المجلس؛ التكامل → قرار إبراهيم |
| **P3 — ميزات موسّعة وتكامل** | بوابة دفع إلكتروني (#2717) · حقول مخصّصة (#2719) · timesheet (#2721) · RFQ→PO (جديد) · POS (جديد) · تكامل حكومي حيّ (جديد) | أدنى | **معماري** | **قرار إبراهيم لكل بند** |

---

## 5) بوابات الحوكمة (تنطبق على كل دفعة عند التنفيذ لاحقًا)

1. **الدفعات التشغيلية** (سلة محذوفات، منع تكرار المورد، إغلاق الحلقة، POD): تُنفَّذ دفعة دفعة داخل المسار المالك → بوابة الدستور + المجلس → تُبلَّغ بالحكم تلقائيًا.
2. **الدفعات التي تمس الدفتر** (خزنة السندات، الفوترة المتكررة، بوابة الدفع، timesheet): **تُحجز لاعتماد إبراهيم + assertion tests على سطور القيد** قبل أي تطبيق.
3. **الدفعات المعمارية** (2FA، قفل التعارض، حقول مخصّصة، RFQ، POS، التكامل الحكومي/البنكي): **هجرة جديدة + قرار إبراهيم** قبل البدء.
4. لا big-bang: دفعة واحدة لكل PR، صفر كتابة عابرة للمسارات، كل تغيير يحمل Audit/Event.

---

## 6) قرارات مطلوبة من إبراهيم (للبدء بالتنفيذ لاحقًا)

- ترتيب البدء: هل نبدأ بـ **P0 (الأمان وسلامة البيانات)** كما هو مقترَح؟
- الموافقة المبدئية على **الهجرات** التي تتطلبها P0+ (جداول 2FA/الجلسات، عمود rowVersion).
- أي بند P3 (POS / RFQ / تكامل حكومي) يُعتمد أصلًا، وأيّها يُؤجَّل أو يُلغى.

## الحالة

- [x] التدقيق الفعلي على الكود (17 قدرة) + تصحيح المسلّمات.
- [x] التقييد: تذاكر #2698 و #2704–#2721 + تذاكر جديدة + تصحيح #2714/#2716.
- [x] الجدولة: هذه الخارطة (P0→P3) + تحديث الفهرس #2710.
- [ ] التنفيذ — **مؤجَّل** بانتظار قرار إبراهيم على ترتيب البدء (القسم 6).
