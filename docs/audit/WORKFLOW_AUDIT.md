# Workflow Integrity Audit — static

Generated: 2026-05-20

> **Read-only.** This file is regenerated by
> `node audit/system-review/tooling/workflow-audit.mjs`. Do not
> hand-edit. Each finding here should turn into an issue or a
> small PR; the audit is the static evidence trail.

## Inventory

| Source | Count |
|---|---|
| `STATE_MACHINES` entries in `lifecycleEngine.ts` | **27** |
| Entities with `lifecycle` block in `entityRegistry.ts` | **32** |
| `applyTransition({ ... })` call sites across `routes/` | **80** |
| Unique entities referenced from routes | **33** |

## 1. Registered but unused state machines

**11** findings.

- **journal_entries** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"draft":["pending_approval","posted","approved","rejected","returned"],"pending_approval":["posted","approved","rejected","returned"],"approved":["posted","rejected"],"posted":[],"rejected":["draft"],"returned":["draft","pending_approval","approved"]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **journal_entries** (column `approvalStatus`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"draft":["pending_review"],"pending_review":["approved","rejected"],"approved":["posted","rejected"],"rejected":["draft"],"posted":[]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **property_contracts** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"draft":["active","cancelled"],"active":["terminated","expired","renewed"],"terminated":[],"expired":["renewed"],"renewed":["active"],"cancelled":[]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **support_tickets** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"open":["in_progress","closed"],"in_progress":["resolved","escalated","closed"],"escalated":["in_progress","resolved","closed"],"resolved":["closed","open"],"closed":["open"]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **workflow_instances** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"draft":["pending","pending_approval"],"pending":["approved","rejected","returned","escalated"],"pending_approval":["approved","rejected","returned"],"returned":["draft","pending","pending_approval"],"escalated":["approved","rejected"],"approved":[],"rejected":[]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **umrah_sales_invoices** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"draft":["approved","cancelled"],"approved":["sent","cancelled"],"sent":["partially_paid","paid","overdue","cancelled"],"partially_paid":["paid","overdue","cancelled"],"paid":[],"overdue":["partially_paid","paid","cancelled"],"cancelled":[]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **umrah_seasons** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"open":["closed"],"closed":["archived"],"archived":[]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **umrah_agents** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"active":["inactive","suspended","blocked"],"inactive":["active"],"suspended":["active","blocked"],"blocked":[]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **governance_policies** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"draft":["active","archived"],"active":["archived","draft"],"archived":[]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **budgets** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"draft":["pending_approval","approved"],"pending_approval":["approved","rejected","returned"],"approved":["closed"],"rejected":["draft"],"returned":["draft","pending_approval"],"closed":[]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

- **financial_periods** (column `status`) — declared in `STATE_MACHINES` but zero route call-sites. Transitions: `{"open":["closed"],"closed":["open"]}`. Either dead, or the entity is mutated via direct SQL (cross-check section 5).

## 2. Used but unregistered (engine trusts route)

**17** findings.

- **hr_inquiry_memos** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/hr-discipline.ts:757`.

- **employee_violations** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/hr.ts:3707`.

- **evaluation_cycles** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/hr.ts:5184`.

- **employee_transfers** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/hr.ts:6118`.

- **hr_excuse_requests** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/hr.ts:7157`.

- **project_phases** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/projects.ts:816`.

- **projects** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/projects.ts:1960`.

- **rental_contracts** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/properties.ts:1448`.

- **maintenance_requests** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/properties.ts:2355`.

- **property_inspections** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/properties.ts:3677`.

- **property_security_deposits** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/properties.ts:3872`.

- **job_postings** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/recruitment.ts:191`.

- **requests** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/requests.ts:533`.

- **training_programs** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/training.ts:214`.

- **umrah_packages** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/umrah.ts:599`.

- **warehouse_products** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/warehouse.ts:454`.

- **inventory_counts** (column `status`) — `applyTransition` is called for this entity but `STATE_MACHINES` has no entry. After PR #654 this is supported (engine bypasses `isValidTransition` and trusts the route's `fromStates`), but each unregistered entity should be a conscious decision. Sample site: `artifacts/api-server/src/routes/warehouse.ts:1450`.

## 3. Route-engine fromState graph mismatch

**0** findings.

_None._

## 4. Registry ↔ engine state-set disagreement

**23** findings.

- **employee_contracts** — has `lifecycle` in `entityRegistry.ts` (states: `["draft","pending_approval","approved","rejected","signed","active","terminated"]`, initial: `draft`, terminals: `["terminated"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **hr_leave_requests** — registry-only states: `[]` ; engine-only states: `["returned"]`. One of the two declarations is stale.

- **hr_overtime_requests** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","rejected"]`, initial: `pending`, terminals: `["rejected"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **hr_official_letters** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","rejected","printed"]`, initial: `pending`, terminals: `["printed"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **hr_loans** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","rejected","active","completed"]`, initial: `pending`, terminals: `["rejected","completed"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **employee_transfers** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","rejected","completed"]`, initial: `pending`, terminals: `["rejected","completed"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **hr_excuse_requests** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","rejected"]`, initial: `pending`, terminals: `["rejected"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **hr_inquiry_memos** — has `lifecycle` in `entityRegistry.ts` (states: `["draft","issued","acknowledged","appealed","escalated","gm_review","justified","closed"]`, initial: `draft`, terminals: `["closed"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **payroll_runs** — has `lifecycle` in `entityRegistry.ts` (states: `["draft","calculated","approved","posted","paid"]`, initial: `draft`, terminals: `["paid"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **journal_entries** — registry-only states: `["reversed"]` ; engine-only states: `["approved","returned"]`. One of the two declarations is stale.

- **purchase_orders** — registry-only states: `[]` ; engine-only states: `["pending","sent","confirmed","invoice_matched","invoice_mismatch","payment_scheduled","returned"]`. One of the two declarations is stale.

- **expense_claims** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","rejected","paid"]`, initial: `pending`, terminals: `["rejected","paid"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **budgets** — registry-only states: `[]` ; engine-only states: `["returned"]`. One of the two declarations is stale.

- **custodies** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","rejected","settled"]`, initial: `pending`, terminals: `["rejected","settled"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **salary_advances** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","rejected","deducted"]`, initial: `pending`, terminals: `["rejected","deducted"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **payment_vouchers** — has `lifecycle` in `entityRegistry.ts` (states: `["draft","approved","posted","cancelled"]`, initial: `draft`, terminals: `["posted","cancelled"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **fleet_trips** — registry-only states: `[]` ; engine-only states: `["planned"]`. One of the two declarations is stale.

- **fleet_insurance_policies** — has `lifecycle` in `entityRegistry.ts` (states: `["active","expired","cancelled","renewed"]`, initial: `active`, terminals: `["cancelled"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **rental_contracts** — has `lifecycle` in `entityRegistry.ts` (states: `["draft","active","terminated","expired","renewed","cancelled"]`, initial: `draft`, terminals: `["terminated","cancelled"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **maintenance_requests** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","approved","in_progress","completed","cancelled"]`, initial: `pending`, terminals: `["completed","cancelled"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **inventory_counts** — has `lifecycle` in `entityRegistry.ts` (states: `["draft","in_progress","pending_approval","approved","cancelled"]`, initial: `draft`, terminals: `["approved","cancelled"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **store_orders** — has `lifecycle` in `entityRegistry.ts` (states: `["pending","confirmed","processing","shipped","delivered","cancelled"]`, initial: `pending`, terminals: `["delivered","cancelled"]`) but NO matching entry in `STATE_MACHINES`. Governance reports will reference states the runtime engine has no opinion on.

- **umrah_sales_invoices** — registry-only states: `["confirmed","partial"]` ; engine-only states: `["approved","sent","partially_paid","overdue"]`. One of the two declarations is stale.

## 5. Direct status UPDATE bypassing the engine

**106** findings.

- `artifacts/api-server/src/routes/admin.ts:1010` (table `audit_violations`): ``UPDATE audit_violations SET status='resolved', "resolvedBy"=$1, "resolvedAt"=NOW() WHERE id=$2 AND "companyId"=$3 AND status != 'resolved'`` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/communications.ts:362` (table `pbx_calls`): ``UPDATE pbx_calls SET status=$1, duration=$2, "recordingUrl"=$3 WHERE id=$4 AND "companyId"=$5 AND status != 'completed'`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/communications.ts:412` (table `pbx_calls`): ``UPDATE pbx_calls SET status=$1, "answeredBy"=$2 WHERE id=$3`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/correspondence.ts:269` (table `correspondence`): ``UPDATE correspondence SET status = 'sent', ${sentField} = NOW(), "updatedAt" = NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/documents.ts:488` (table `documents`): ``UPDATE documents SET status=$1, "updatedAt"=NOW() WHERE id=$2 AND "companyId"=$3 AND status != $1 AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:757` (table `onboarding_tasks`): ``UPDATE onboarding_tasks SET status = $1,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1101` (table `employee_assignments`): ``UPDATE employee_assignments SET status = 'active' WHERE id = $1 AND "companyId" = $2 AND status = $3`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1106` (table `employee_assignments`): ``UPDATE employee_assignments SET status = 'suspended' WHERE id = $1 AND "companyId" = $2 AND status = $3`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1268` (table `employee_assignments`): ``UPDATE employee_assignments SET status = 'terminated' WHERE id = $1 AND "companyId" = $2 AND status = 'active'`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1272` (table `employees`): ``UPDATE employees SET status = 'terminated' WHERE id = $1 AND "companyId" = $2 AND status = 'active' AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1279` (table `employee_contracts`): ``UPDATE employee_contracts` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1288` (table `hr_leave_requests`): ``UPDATE hr_leave_requests` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1294` (table `leave_approval_stages`): ``UPDATE leave_approval_stages` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1307` (table `tasks`): ``UPDATE tasks` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1317` (table `approval_requests`): ``UPDATE approval_requests` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/employees.ts:1325` (table `hr_employee_loans`): ``UPDATE hr_employee_loans` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-algorithms.ts:464` (table `bank_statements`): ``UPDATE bank_statements SET "matchStatus" = 'matched', "matchedJournalLineId" = $1 WHERE id = $2 AND "companyId" = $3`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-algorithms.ts:566` (table `bank_statements`): ``UPDATE bank_statements SET "matchStatus"='matched', "matchedJournalLineId"=$1 WHERE id=$2 AND "companyId"=$3`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-cost-centers.ts:164` (table `cost_centers`): ``UPDATE cost_centers SET status = 'deleted', "updatedAt" = NOW() WHERE id = $1 AND "companyId" = $2 AND status != 'deleted'`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-custodies.ts:609` (table `journal_entries`): ``UPDATE journal_entries SET status = 'pending_approval' WHERE id = $1 AND "companyId" = $2 AND status = 'draft' AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-invoices.ts:752` (table `invoices`): ``UPDATE invoices SET "paidAmount" = $1, status = $2, "paidAt" = $3 WHERE id = $4 AND "companyId" = $5 AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-invoices.ts:757` (table `invoices`): ``UPDATE invoices SET "paidAmount" = $1, status = $2 WHERE id = $3 AND "companyId" = $4 AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-invoices.ts:963` (table `journal_entries`): ``UPDATE journal_entries SET "deletedAt" = NOW(), status = 'cancelled' WHERE id = $1 AND "companyId" = $2 AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-invoices.ts:968` (table `invoices`): ``UPDATE invoices SET "deletedAt" = NOW(), status = 'cancelled' WHERE id = $1 AND "companyId" = $2 AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-invoices.ts:1034` (table `journal_entries`): ``UPDATE journal_entries SET status = 'cancelled' WHERE id = $1 AND "companyId" = $2 AND status IN ('posted', 'approved') AND "deletedAt" IS ` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-invoices.ts:1197` (table `invoices`): ``UPDATE invoices SET "paidAmount" = COALESCE("paidAmount",0) + $1,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-invoices.ts:1733` (table `customer_advances`): ``UPDATE customer_advances SET "appliedAmount" = COALESCE("appliedAmount",0) + $1,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-invoices.ts:1739` (table `invoices`): ``UPDATE invoices SET "paidAmount" = COALESCE("paidAmount",0) + $1,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-journal.ts:528` (table `journal_entries`): `if (approvalResult.requiresApproval) { await rawExecute(`UPDATE journal_entries SET status = 'pending_approval' WHERE id = $1 AND "companyId` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-journal.ts:939` (table `journal_entries`): `if (approvalResult.requiresApproval) { const { affectedRows } = await rawExecute(`UPDATE journal_entries SET status = 'pending_approval' WHE` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-journal.ts:1405` (table `financial_periods`): ``UPDATE financial_periods SET status='closed', "closedAt"=NOW(), "closedBy"=$1, "updatedAt"=NOW() WHERE id=$2 AND "companyId"=$3 AND status ` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-zatca.ts:490` (table `zatca_settings`): ``UPDATE zatca_settings SET "lastConnectionTest" = NOW(), "connectionTestStatus" = $1, "connectionTestMessage" = $2 WHERE "companyId" = $3`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-zatca.ts:687` (table `invoices`): ``UPDATE invoices SET "zatcaUuid" = $1::uuid, "zatcaHash" = $2, "zatcaStatus" = $3, "zatcaQrCode" = $4` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/finance-zatca.ts:775` (table `journal_entries`): ``UPDATE journal_entries SET "zatcaUuid" = $1::uuid, "zatcaHash" = $2, "zatcaStatus" = $3, "zatcaQrCode" = $4` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1077` (table `fleet_vehicles`): `const vResult = await client.query(`UPDATE fleet_vehicles SET status='in_use', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status='` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1081` (table `fleet_drivers`): `const dResult = await client.query(`UPDATE fleet_drivers SET status='on_trip' WHERE id=$1 AND "companyId"=$2 AND status='available' AND "del` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1173` (table `fleet_vehicles`): `const vRes = await client.query(`UPDATE fleet_vehicles SET status='available', "currentMileage"="currentMileage"+$1, "updatedAt"=NOW() WHERE` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1177` (table `fleet_drivers`): `const dRes = await client.query(`UPDATE fleet_drivers SET status='available', "totalTrips"=COALESCE("totalTrips",0)+1 WHERE id=$1 AND "compa` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1256` (table `fleet_vehicles`): ``UPDATE fleet_vehicles SET status='available', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status='in_use' AND "deletedAt" IS NULL`` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1262` (table `fleet_drivers`): ``UPDATE fleet_drivers SET status='available' WHERE id=$1 AND "companyId"=$2 AND status='on_trip' AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1413` (table `fleet_vehicles`): `await client.query(`UPDATE fleet_vehicles SET status='maintenance', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status='available' ` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1514` (table `fleet_vehicles`): `await client.query(`UPDATE fleet_vehicles SET status='available', "lastMaintenanceDate"=NOW(), "updatedAt"=NOW() WHERE id=$1 AND "companyId"` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:1601` (table `fleet_vehicles`): `await client.query(`UPDATE fleet_vehicles SET status='available', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status='maintenance' ` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:2115` (table `fleet_vehicles`): ``UPDATE fleet_vehicles SET status='available', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status IN ('in_use','on_trip') AND "dele` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:2121` (table `fleet_drivers`): ``UPDATE fleet_drivers SET status='available', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status='on_trip' AND "deletedAt" IS NULL`` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/fleet.ts:2267` (table `fleet_vehicles`): ``UPDATE fleet_vehicles SET status='available', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status='maintenance' AND "deletedAt" IS ` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/gov-integrations.ts:282` (table `gov_integrations`): ``UPDATE gov_integrations SET "lastCheckedAt"=NOW(), "lastCheckStatus"=$2, "lastCheckMessage"=$3, "updatedAt"=NOW() WHERE id=$1 AND "companyI` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/gov-integrations.ts:337` (table `gov_integrations`): ``UPDATE gov_integrations SET "lastCheckedAt"=NOW(), "lastCheckStatus"=$2, "lastCheckMessage"=$3, "updatedAt"=NOW() WHERE id=$1 AND "companyI` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/governance.ts:331` (table `governance_policies`): ``UPDATE governance_policies SET status='archived', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status IN ('draft','active') AND "de` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-contracts.ts:259` (table `employee_contracts`): ``UPDATE employee_contracts SET "approvalStatus" = 'pending_approval', "updatedAt" = NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-contracts.ts:300` (table `employee_contracts`): ``UPDATE employee_contracts` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-contracts.ts:346` (table `employee_contracts`): ``UPDATE employee_contracts` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-contracts.ts:489` (table `employee_contracts`): ``UPDATE employee_contracts` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-contracts.ts:533` (table `employee_contracts`): ``UPDATE employee_contracts` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-discipline.ts:919` (table `employee_violations`): ``UPDATE employee_violations SET status = 'rejected' WHERE id = $1 AND "companyId" = $2 AND status = 'pending' AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-discipline.ts:944` (table `employee_violations`): ``UPDATE employee_violations` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-discipline.ts:1013` (table `employee_violations`): ``UPDATE employee_violations SET status = 'cancelled'` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-discipline.ts:1109` (table `employee_violations`): ``UPDATE employee_violations SET status = 'appeal_accepted' WHERE id = $1 AND "companyId" = $2 AND status = 'approved' AND "deletedAt" IS NUL` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-discipline.ts:1164` (table `employee_violations`): ``UPDATE employee_violations SET status = 'closed' WHERE id = $1 AND "companyId" = $2 AND status IN ('approved', 'rejected', 'appeal_accepted` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-exit.ts:534` (table `hr_exit_clearance`): ``UPDATE hr_exit_clearance` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-exit.ts:592` (table `employee_assignments`): ``UPDATE employee_assignments SET status = 'terminated', "endDate" = CURRENT_DATE` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-loans.ts:440` (table `hr_employee_loans`): ``UPDATE hr_employee_loans SET status = 'rejected', "rejectionReason" = $1, "updatedAt" = NOW() WHERE id = $2 AND "companyId" = $3 AND status` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-loans.ts:486` (table `hr_employee_loans`): ``UPDATE hr_employee_loans` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-loans.ts:591` (table `hr_employee_loans`): ``UPDATE hr_employee_loans SET status = 'rejected', "rejectionReason" = $1, "updatedAt" = NOW() WHERE id = $2 AND "companyId" = $3 AND status` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-overtime.ts:401` (table `hr_overtime_requests`): ``UPDATE hr_overtime_requests SET status = 'rejected', "rejectionReason" = $1, "updatedAt" = NOW() WHERE id = $2 AND "companyId" = $3 AND sta` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-overtime.ts:447` (table `hr_overtime_requests`): ``UPDATE hr_overtime_requests` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr-overtime.ts:515` (table `hr_overtime_requests`): ``UPDATE hr_overtime_requests SET status = 'rejected', "rejectionReason" = $1, "updatedAt" = NOW() WHERE id = $2 AND "companyId" = $3 AND sta` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:1847` (table `leave_approval_stages`): ``UPDATE leave_approval_stages` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:1899` (table `leave_approval_stages`): ``UPDATE leave_approval_stages SET status = 'returned', decision = $1, "decidedBy" = $2, "decidedAt" = NOW() WHERE id = $3 AND status = 'pend` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:1961` (table `leave_approval_stages`): ``UPDATE leave_approval_stages` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:2029` (table `leave_approval_stages`): ``UPDATE leave_approval_stages` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:2048` (table `approval_requests`): ``UPDATE approval_requests SET status = 'approved', "decidedBy" = $1, "decidedAt" = NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:2253` (table `leave_approval_stages`): ``UPDATE leave_approval_stages SET status = 'escalated'` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:2698` (table `attendance_deductions`): ``UPDATE attendance_deductions SET status = 'deducted_in_payroll'` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:2717` (table `hr_loan_installments`): ``UPDATE hr_loan_installments SET status = 'paid', "paidAt" = NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:2737` (table `hr_overtime_requests`): ``UPDATE hr_overtime_requests SET status = 'paid', "updatedAt" = NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:2827` (table `payroll_runs`): ``UPDATE payroll_runs SET status='completed', "approvedBy"=$1, "approvedAt"=NOW() WHERE id=$2 AND "companyId"=$3 AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:3499` (table `email_queue`): ``UPDATE email_queue SET status='cancelled', "errorMessage"='تم رفض الخطاب الرسمي', "updatedAt"=NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:3504` (table `whatsapp_queue`): ``UPDATE whatsapp_queue SET status='cancelled', "errorMessage"='تم رفض الخطاب الرسمي', "updatedAt"=NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:3920` (table `official_letters`): ``UPDATE official_letters SET status = 'pending_approval' WHERE id = $1 AND "companyId" = $2 AND status = 'draft' AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4216` (table `payroll_runs`): ``UPDATE payroll_runs SET status = $1 WHERE id = $2 AND "companyId" = $3 AND "deletedAt" IS NULL AND status = $4 RETURNING *`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4300` (table `payroll_runs`): ``UPDATE payroll_runs SET status = $1 WHERE id = $2 AND "companyId" = $3 AND "deletedAt" IS NULL AND status = $4 RETURNING *`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4334` (table `attendance_deductions`): ``UPDATE attendance_deductions SET status = 'pending_payroll' WHERE period = $1 AND "companyId" = $2 AND status = 'deducted_in_payroll'`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4345` (table `hr_loan_installments`): ``UPDATE hr_loan_installments SET status = 'pending' WHERE "payrollLineId" IN (SELECT id FROM payroll_lines WHERE "runId" = $1) AND "companyI` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4358` (table `hr_overtime_requests`): ``UPDATE hr_overtime_requests SET status = 'approved' WHERE "payrollLineId" IN (SELECT id FROM payroll_lines WHERE "runId" = $1) AND "company` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4591` (table `official_letters`): ``UPDATE official_letters` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4598` (table `official_letters`): ``UPDATE official_letters SET status = $1 WHERE id = $2 AND "companyId" = $3 AND status = 'pending_approval' AND "deletedAt" IS NULL`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4607` (table `email_queue`): ``UPDATE email_queue SET status='cancelled', "errorMessage"='تم رفض الخطاب الرسمي', "updatedAt"=NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/hr.ts:4612` (table `whatsapp_queue`): ``UPDATE whatsapp_queue SET status='cancelled', "errorMessage"='تم رفض الخطاب الرسمي', "updatedAt"=NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/obligations.ts:136` (table `obligations`): ``UPDATE obligations SET status='met', "metAt"=NOW(), "updatedAt"=NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/obligations.ts:172` (table `obligations`): ``UPDATE obligations SET status='cancelled', "updatedAt"=NOW()` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/print.ts:573` (table `print_reprint_requests`): ``UPDATE print_reprint_requests` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/print.ts:597` (table `print_reprint_requests`): ``UPDATE print_reprint_requests` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/projects.ts:942` (table `project_tasks`): `await client.query(`UPDATE project_tasks SET status='blocked' WHERE id=$1 AND status='todo' AND "deletedAt" IS NULL AND "projectId" IN (SELE` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/projects.ts:1085` (table `project_tasks`): ``UPDATE project_tasks SET status='todo'` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/properties.ts:1583` (table `contract_payment_schedule`): ``UPDATE contract_payment_schedule SET status='cancelled'` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/properties.ts:1591` (table `property_units`): ``UPDATE property_units SET status='available', "updatedAt"=NOW() WHERE id=$1 AND "companyId"=$2 AND status IN ('occupied','rented') AND "del` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/properties.ts:3525` (table `contract_payment_schedule`): ``UPDATE contract_payment_schedule SET "paidAmount"=$1, "paidDate"=$2, method=$3, status=$4, "receiptNumber"=$5, "updatedAt"=NOW() WHERE id=$` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/rbacV2.ts:1049` (table `rbac_jit_requests`): ``UPDATE rbac_jit_requests` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/rbacV2.ts:1111` (table `rbac_jit_requests`): ``UPDATE rbac_jit_requests` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/rbacV2.ts:1162` (table `rbac_jit_requests`): ``UPDATE rbac_jit_requests` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/recruitment.ts:207` (table `job_applications`): ``UPDATE job_applications` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/settings.ts:510` (table `branches`): ``UPDATE branches SET status='inactive' WHERE id=$1 AND "companyId"=$2`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/umrah-entities.ts:1524` (table `official_letters`): ``UPDATE official_letters` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/umrah.ts:980` (table `umrah_import_logs`): ``UPDATE umrah_import_logs SET "newRecords"=$1,"updatedRecords"=$2,"duplicateRecords"=$3,"errorRecords"=$4,errors=$5,"processedRows"=$6,statu` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

- `artifacts/api-server/src/routes/umrah.ts:1393` (table `umrah_penalties`): ``UPDATE umrah_penalties SET status='invoiced', "invoiceId"=$1 WHERE "agentId"=$2 AND "seasonId"=$3 AND "companyId"=$4 AND status='pending'`,` — bypasses `applyTransition` ⇒ no engine state-validation, no audit log entry, no event emission, no lifecycle side-effects.

## 6. Inconsistent fromStates across routes

**0** findings.

_None._

## Reproducing this audit

```bash
node audit/system-review/tooling/workflow-audit.mjs
```

Re-running regenerates both this file and
`audit/system-review/tooling/_workflow-audit.json`. The script is
read-only — it touches no application code.
