# إعادة فحص جرد العيوب — RESCAN v3 (2026-05-22)

> **النوع:** تحديث حالة بعد دمج الدفعة الثانية من إصلاحات الموجة الثانية.
> **المرجع:** `main` بعد PR #868 · #869 · #874 · #875.
> **الطريقة:** تأكيد الإصلاحات الأربعة في مكانها + تحديث قائمة العمل المتبقية.

## 1. الدفعة الثانية من الموجة الثانية (4 عيوب — مدموجة)

| ID | المسار | الإصلاح | PR |
|---|---|---|---|
| WH-004 | Warehouses | `adjustment` فُصِل إلى `adjustment_in`/`adjustment_out` باتجاه ومُحفِّز GL لحساب فروقات الجرد | #868 |
| SUP-015 | Support | دالة تصعيد SLA موحّدة (`lib/supportSlaEscalation.ts` · `escalateSla`) — المسارات الأربعة تمرّ عبرها | #869 |
| FND-003 | Foundation | كل قراءات `process.env` نُقلت إلى `config`؛ حارس lint `direct-process-env-read` | #874 |
| SUP-006 | Support | محدِّد حالة التذكرة يعرض الانتقالات المسموحة فقط من آلة الحالات | #875 |

> الموجة الثانية حتى الآن: 17 (الدفعة الأولى) + 4 (هذه) = **21 عيبًا مدموجًا**.

## 2. قائمة عمل الموجة الثانية المتبقية

**إصلاح بنيوي متبقٍّ:**
- **FND-010** — كتالوجا RBAC متوازيان (`lib/rbacCatalog.ts` المسطّح ولا يزال مستهلَكًا من 28 ملفًا، مقابل `lib/rbac/featureCatalog.ts` الشجري). توحيدهما تحت `featureCatalog` يمسّ نواة الصلاحيات — يحتاج جلسة مركّزة لمخاطر الانحدار الأمني.

**ميزات «بناء واجهة / دمج أنظمة» (أكبر من رقعة، كل واحدة بحجم مشروع صغير):**
- FLT-006 — جدول `fleet_alerts` + واجهته.
- FIN-013 — أزرار اعتماد/ترحيل في `journal-manual-detail`.
- FIN-014 — واجهة إقفال الفترة المالية.
- FIN-015 — توحيد نظامَي الفترات المالية v1/v2.
- FIN-016 — واجهة دورة GRN/مطابقة/دفع.
- CRM-004 — صفحة نشاطات الفرص + زر إنشاء.
- COM-001 — واجهة تعديل/حذف سجل الاتصالات.
- COM-002 — واجهة إنشاء/حذف قواعد التوجيه.
- UMR-005 — صفحة فواتير nusk.
- UMR-016 — صفحة قائمة فواتير مبيعات العمرة.
- HR-010 — واجهات attendance-policy/accruals/delegations/approval-chains.
- HR-013 — قرار منتج: توحيد كيانَي المخالفات (`hr_inquiry_memos` مقابل `employee_violations`).

## 3. ملاحظات متابعة (دون تغيير عن v2)

- دفتر الأستاذ `/ledger/:code` يفلتر `je.status='posted'` فلا يُظهر بنود السندات المعتمَدة (`approved`).
- COM-016: يتبقّى تكرار بنيوي (مساران لـ`/preferences` بنموذجَي بيانات).

---

*انتهت إعادة الفحص v3. تدقيق ثابت فقط.*
