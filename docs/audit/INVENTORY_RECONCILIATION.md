# مطابقة الجرد مع المستودع الحالي — INVENTORY_RECONCILIATION

> **النوع:** تصحيح توثيقي — جولة مطابقة بعد الجرد. لا تعديل كود، لا إصلاح، لا PR.
> **التاريخ:** 2026-05-21 · **المستودع:** `barhom64/ghayth-erp` · **الفرع:** `claude/nice-ride-xWdwq`
> **لقطة الجرد الأصلي:** حتى التعديل **#778**. **لقطة المطابقة:** `main` حتى التعديل **#816**.
> **النطاق:** فحص diff-by-diff لكل التعديلات الـ32 المدموجة بعد #778 (#779–#816)، ومطابقتها بالعيوب الـ184 في `SYSTEM_INVENTORY_MATRIX.md`.

---

## 1. الخلاصة بلغة بسيطة

الجرد الأصلي صوّر النظام عند لحظة معيّنة (التعديل #778). بعد تلك اللحظة بثلاث ساعات تقريبًا، دُمج **32 تعديلًا** — موجة إصلاح متوازية للموارد البشرية والطبقة العرضية والمراقبة كانت تعمل بالتزامن مع التدقيق.

نتيجة الفحص الدقيق: **11 عيبًا من الـ184 تغيّرت حالته** — 5 عُولِجت بالكامل، 6 عُولِجت جزئيًا. الباقي (173 عيبًا) لم يمسّه شيء ولا يزال قائمًا كما وُصِف.

والأهمّ: **الثقوب المالية الأربعة (FIN-001/002/003/008) لم تُمَسّ** — كل تنبيهات الجرد المالية الحرجة قائمة بالكامل. كما كشفت موجة الإصلاح **ثلاثة عيوب فاتت الجردَ نفسه** — اكتُشفت بالتشغيل الحيّ وعُولِجت.

**القراءة العملية:** الجرد ما زال مرجعًا صحيحًا لـ ~94% من محتواه. التصحيح المطلوب محصور في 11 عيبًا، معظمها في الموارد البشرية.

---

## 2. العيوب التي تغيّرت حالتها (11 عيبًا)

التشخيص أدناه مبنيٌّ على قراءة الـ diff الفعلي لكل تعديل، لا على عنوانه.

### 2.1 عُولِجت بالكامل — 5 عيوب

| العيب | كان | التعديل المُعالِج | الدليل من الـ diff |
|---|---|---|---|
| **HR-004** | penalty-escalation تُصفّي على حالة `active` غير موجودة → صفحة فارغة دائمًا | #789 | الفلتر صُحِّح إلى «غير مرفوضة»، والصفحة تقرأ لائحة الجزاءات الفعلية وتعرض سلّم تصعيد من أربع مراحل مبنيًّا على `hr_discipline_regulation`. |
| **HR-006** | salary-components صفحة إنشاء فقط؛ عمود الحالة ملفّق | #785 | أُضيف `PATCH` و`DELETE` للمكوّن في `hr.ts` مع audit وevent، والواجهة تشتقّ الحالة من `isActive` الحقيقي وتعرض أزرار تعديل/تعطيل/حذف. |
| **HR-007** | حقل `gracePeriod` يُرسَل ولا يُدرَج | #782 | المهاجرة 186 أضافت العمودين `gracePeriod`/`breakMinutes` إلى جدول الورديات، وINSERT يُدرجهما الآن، وحُدِّث `schema_pre.sql`. |
| **HR-008** | `objectives`/`targetAudience` يُسقطهما INSERT التدريب | #782 | المهاجرة 186 أضافت العمودين إلى `training_programs`، وINSERT في `training.ts` يُدرجهما. |
| **HR-012** | `/hr/development-plans` مجرّد إعادة تصدير لصفحة IDP — مساران لصفحة واحدة | #786 | حُذف ملف إعادة التصدير، ومساره في `hrRoutes.tsx`، وإدخاله المكرّر في الشريط الجانبي. |

### 2.2 عُولِجت جزئيًا — 6 عيوب

| العيب | كان | التعديل | ما عولج وما بقي |
|---|---|---|---|
| **HR-003** | تحوّلات `employee_violations` بلا فحص حالة مبدئية | #779 | **عولج جزئيًا:** التعديل #779 حوّل خمس نقاط `UPDATE` خام في مسار المذكرات (`hr-discipline.ts`) إلى `applyTransition` بـ`fromStates`. **بقي:** دالة `violationApprovalAction` في `hr.ts` (موضع العيب الأصلي) لم تدخل نطاق #779 — تحتاج تأكيدًا مستقلًّا. |
| **HR-010** | سطح API كبير بلا واجهة (سياسة الحضور، الاستحقاقات، التفويضات، سلاسل الاعتماد...) | #792 | **عولج جزئيًا:** أُضيفت واجهة إدارة `approval-chain-definitions` (إنشاء/عرض/حذف). **بقي:** attendance-policy، accruals، delegations، onboarding-steps/tasks، training/enrollments، discipline/penalty-preview — كلها لا تزال بلا واجهة. |
| **HR-015** | لا آلة حالة مُسجَّلة لجداول دورة حياة HR | #797 + #803 | **عولج جزئيًا:** سُجِّلت أربع آلات حالة (`payroll_runs`، `hr_employee_loans`، `hr_overtime_requests`، `employee_contracts`)، وصُحِّحت اثنتان منها في #803. **بقي:** المسارات نفسها لا تستدعي `applyTransition` بعد (التسجيل توثيقي/دفاعي فقط)، و`job_postings` غير مُسجَّل. |
| **FND-003** | 68+ قراءة `process.env` متناثرة تنقض عقد «المصدر الوحيد» | #781 + #783 | **عولج جزئيًا:** حُوِّلت أربع وحدات (Redis، SIEM، ZATCA retry، e-invoice registry) إلى `config`. **بقي:** الغالبية العظمى من القراءات المتناثرة (تشفير الحقول، الأسرار، CORS...) لم تُحوَّل بعد. |
| **FND-008** | فشل cron يُسجَّل ولا يُنبَّه ولا واجهة إعادة تشغيل | #809 + #810 | **عولج جزئيًا:** #809 أضاف طبقة تنبيهات عتبية لمؤشّرات التشغيل (تأخّر حلقة الأحداث، المعالج، تكدّس اتصالات القاعدة)، و#810 أضاف نقطة `/health/system` تعرض «عدد إخفاقات cron في آخر ساعة». **بقي:** لا تنبيه مخصّص لفشل وظيفة cron بعينها، ولا واجهة إعادة تشغيل. |
| **FND-013** | `buildScopedWhere` غير مُتبنّى — تطبيع النطاق #685 | #798 | **عولج جزئيًا:** حُوِّلت قائمتا `legal` إلى `buildScopedWhere`. **بقي:** بقية الملفات؛ كما أن رقم «17 ملفًا / 68 محمولًا» في الجرد مأخوذ من تقرير #685 سابق ويحتاج إعادة عدّ — مسار #685 يتقدّم بمعزل عن هذا الجرد. |

---

## 3. عيوب فاتت الجردَ نفسه — اكتُشفت وعُولِجت بعد #778

الفحص كشف ثلاثة عيوب حقيقية **لم يلتقطها جردي الأصلي**، اكتشفها التحقّق التشغيلي الحيّ للموجة الموازية، وعُولِجت كلها. أُسجّلها للحياد والاكتمال:

1. **محرف `%` في فلتر `applyTransition` يكسر اعتمادات المصروفات/العهد/السندات/السلف.** كان محرّك دورة الحياة يرفض أي فلتر يحوي `%` (المستخدَم في `LIKE 'PREFIX%'`) بخطأ HTTP 400 — فكل اعتماد مصروف أو عهدة أو سند أو سلفة يفشل. عُولِج بالتعديل #816. **هذا عيب blocking فاتني** — كان يجب أن يظهر في جرد المالية. ملاحظة دقيقة: هذا العيب كان يحجب اعتماد السندات قبل أن يصل التنفيذ أصلًا إلى عيب FIN-006؛ بعد #816 أصبح FIN-006 (عدم تطابق بادئة `ref`) هو الحاجب الفعّال المتبقّي.
2. **تعارض قفل (deadlock) كامن في كل نقاط كتابة مذكرات التأديب.** دالة تسجيل حدث المذكرة كانت تفتح اتصالًا منفصلًا بقاعدة البيانات يتعارض مع القفل الذي يمسكه التحوّل الأب — تجمّد دائم. اكتشفه واصلحه #779. **عيب فاتني** (سلوك runtime لا يظهر بوضوح من الكود الثابت — كان مرشّحًا لقسم «يحتاج Runtime Verification»).
3. **كتابات تشغيل القيد المتكرر غير ذرّية.** عُولِج بالتعديل #787 (لفّها في معاملة واحدة). عيب سلامة معاملات فاتني.

**دلالة هذا:** الجرد الثابت له حدّ — ثلاثة عيوب blocking/structural ظهرت فقط بالتشغيل الحيّ. هذا يؤكّد ما ورد في أقسام «يحتاج Runtime Verification» في ملفات الجرد: التدقيق الثابت لا يُغني عن التحقّق التشغيلي.

---

## 4. عيوب حرجة مؤكَّد أنها ما زالت قائمة

تحقّقتُ مباشرةً أن أيًّا من التعديلات الـ32 لم يمسّ هذه — تنبيهات الجرد بشأنها قائمة بالكامل:

- **FIN-001 / FIN-002 / FIN-003 / FIN-008** — الثقوب المالية الأربعة. التعديلات المالية اللاحقة عالجت أمورًا مجاورة لا هذه: #784 و#811 صحّحا تواريخ فحص الفترة المالية، و#801 منع الترحيل على حساب معطّل — **ولا واحد منها يلمس سلسلة الشراء GRNI/AP ولا التسوية البنكية**. قائمة بالكامل.
- **FIN-006** — عدم تطابق بادئة `ref` في اعتماد السند. كشف #816 أنه كان محجوبًا بعيب آخر (محرف `%`)؛ بعد إصلاح ذلك أصبح FIN-006 هو الحاجب الفعّال. قائم.
- **CRM-013** (تعارض `clients`)، **WH-007** (حركات بلا `branchId`)، **PROP-001/002/003/004**، **FLT-001/010**، **PRJ-001/002/003**، **COM-003/016**، **HR-005** (جسر التوظيف→موظف)، **FND-001** (مجلّد migrations الميت) — كلها قائمة، لم يمسّها شيء.
- **كامل مسارات** العقارات والأسطول والمستودعات والمشاريع والعملاء والدعم والاتصالات والعمرة — موجة ما بعد #778 كانت محصورة في HR + Foundation + حوكمة المالية + المراقبة؛ لم تُدقَّق فيها أي من هذه المسارات السبعة، وكل عيوبها قائمة.

---

## 5. ملاحظة على الـ173 عيبًا غير المتغيّرة

173 عيبًا من الـ184 لم تتأثّر إطلاقًا. لا تحتاج إعادة تحقّق بسبب هذه الموجة — يبقى تشخيصها كما في `SYSTEM_INVENTORY_MATRIX.md`. أي إعادة تحقّق مستقبلية لها سببها مرور الوقت لا هذه التعديلات.

---

## 6. منهجية ولماذا لم أُعدّل الملفات الـ15

- **لم أُعدّل ملفات الجرد الـ15 الأصلية.** هي لقطة مؤرّخة بدقّة عند #778؛ تعديلها يُفقدها قيمتها كمرجع تاريخي ثابت. هذا الملف هو **طبقة المطابقة** فوقها: الجرد يبقى لقطةً، وهذه الوثيقة تسجّل ما تغيّر بعدها.
- **كل تشخيص هنا مبنيٌّ على قراءة الـ diff الفعلي** للتعديل (رسالة التعديل + تغييرات الكود)، لا على العنوان وحده.
- **حدّ هذه الجولة:** فحصتُ التعديلات #779–#816. أي تعديل يُدمج بعد #816 يحتاج جولة مطابقة جديدة.

---

## 7. التوصية

1. **اعتمد `SYSTEM_INVENTORY_MATRIX.md` + هذا الملف معًا** كمرجع موحَّد: المصفوفة للتشخيص، وهذه الوثيقة لتصحيح حالة 11 عيبًا.
2. **عند توزيع موجة الإصلاح:** احذف من القائمة العيوب الخمسة المُعالَجة بالكامل (HR-004/006/007/008/012)، وعامِل الستّة المُعالَجة جزئيًا على أساس «ما بقي» فقط (العمود الأخير في جدول 2.2).
3. **الأولوية القصوى ما زالت كما في التقرير التنفيذي:** الثقوب المالية الأربعة — لم تتغيّر، ولم يصلها أحد بعد.
4. **جولة مطابقة دورية:** كلما دُمجت دفعة تعديلات جديدة، تُعاد هذه الجولة قبل اعتماد المصفوفة في أي قرار توزيع — وإلا تكرّر تقادم اليوم.

*انتهت وثيقة المطابقة — تصحيح توثيقي، تدقيق ثابت، لا تشغيل ولا تعديل كود.*
