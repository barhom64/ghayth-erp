# إعادة فحص جرد العيوب — RESCAN v2 (2026-05-22)

> **النوع:** إعادة تحقّق ثانية بعد دمج 10 إصلاحات من الموجة الثانية.
> **المرجع:** `main` @ `f8513c2` (بعد PR #853–#862).
> **الطريقة:** ثلاثة مسارات تحقّق متوازية؛ تأكيد إصلاحات الموجة الثانية + إعادة فحص بقية العيوب + رصد الانحدارات.

## 1. تأكيد إصلاحات الموجة الثانية (10/10 مؤكَّدة)

HR-001 · HR-002 · HR-003 · FIN-005 · FIN-007 · CRM-003 · COM-016 · SUP-016 · COM-011 · PRJ-005 — جميعها مؤكَّدة في مكانها على `main` دون انحدار في الإصلاح نفسه.

## 2. انحدار واحد مكتشَف (وأُصلح)

- **انحدار من FIN-007** — `DELETE /vouchers/:id` كان يعكس أرصدة سند `draft` مؤجَّل لم تُطبَّق أصلًا، فيُفسد `currentBalance`. أُصلح: `reverseAccountBalances` يتخطّى القيود ذات `balancesApplied = false` ويضبط العلم بعد العكس (PR #863).

## 3. تصحيحات على الجرد

- **PROP-003** — مُصلَح فعليًا (الجرد و RESCAN الأول صنّفاه قائمًا): محمولات `rent_payments.companyId/deletedAt` الخاطئة أُزيلت سلفًا (`properties.ts:1899-1903,1947-1954,2060-2090`). أدلّة `properties.md` قديمة.
- **SUP-006** — يُعاد تحجيمه إلى عيب واجهة فقط: SUP-016 حلّ تعارض الخادم؛ يبقى أن مُحدِّد الحالة في `ticket-detail.tsx` يعرض انتقالات غير مسموحة.
- **COM-016** — الجزء الحاجب (`ON CONFLICT`) أُصلح؛ يبقى التكرار البنيوي (مساران لـ`/preferences` بنموذجَي بيانات).
- الإيجابيات الكاذبة PROP-002 · PROP-004 · WH-005 · FIN-008 — مؤكَّدة ثانيةً.

## 4. قائمة عمل الموجة الثانية المتبقية

**إصلاحات بنيوية نظيفة (تُنفَّذ تباعًا):**
- FND-004 — routers حسّاسة بلا `requireModule`/`requireMinLevel` (`routes/index.ts`).
- FND-008 — فشل cron بلا تنبيه تشغيلي ولا إعادة تشغيل (`cronScheduler.ts`).
- FND-003 — 77 قراءة `process.env` في 37 ملفًا خارج `config.ts`.
- FND-010 — كتالوجا RBAC متوازيان.
- FLT-003 — TCO يعتمد عمودَي شراء غير موجودَين (يحتاج هجرة).
- FLT-006 — تنبيهات الأسطول محسوبة لحظيًا بلا جدول.
- WH-004 — نوع الحركة `adjustment` بلا تفريع ولا GL.
- SUP-006 (واجهة) · SUP-015 (توحيد قواعد تصعيد SLA).

**ميزات «بناء واجهة / دمج أنظمة» (أكبر من رقعة):** HR-010 · HR-013 · FIN-013/014/015/016 · CRM-004 · COM-001/002 · UMR-005/016.

---

*انتهت إعادة الفحص v2. تدقيق ثابت فقط.*
