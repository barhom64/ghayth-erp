# 12 — العناصر الميتة والقديمة

> «ميت» = لا مستخدم له بدليل؛ «قديم» = حيّ لكنه من طبقة متجاوَزة. كل بند بدليله وإجراءه.

---

| # | العنصر | الموقع | الدليل | التصنيف | الإجراء + معيار القبول | الأولوية |
|---|---|---|---|---|---|---|
| DEAD-01 | صفحة «رحلة جديدة» | `create/fleet/trips-create.tsx` | redirect 5 ثوانٍ + اختبار إهلاك قائم | مسار قديم (4) | إزالة الملف + المسار بعد دورة سماح؛ القبول: لا route ولا ملف واختبار محدَّث | متوسط |
| DEAD-02 | stubs مزوّدي خرائط | enum `mapProvider`: mapbox / here_maps | لا implementation — adapters غير موجودة | منطق موهِم (11) | إخفاء الخيارين من واجهة الإعدادات أو وسم «غير مفعّل»؛ القبول: المشغّل لا يستطيع اختيار مزوّد غير عامل | متوسط |
| DEAD-03 | مفردات حالة قديمة في الواجهات | `vehicle-context-card` (active/retired/sold) و`me-driver` CARGO_STATUS (confirmed/loading/closed) | لا تطابق enums الجداول الحالية — تسقط على fallback خام | قالب قديم (5) | استبدالها بقواميس الحالة الموحَّدة (خطة 14 §3)؛ القبول: صفر fallback إنجليزي | مهم |
| DEAD-04 | تعليق cron التجسيد | رأس `transport-route-patterns.ts` | الوظيفة غير موجودة (TA-CONF-01) | توثيق ميت | يُصحَّح ضمن بناء الـcron (TA-T18-06) | عاجل (ضمن 06) |
| LEGACY-01 | طبقة fleet_trips | الجدول + endpoints + صفحتا trips | حيّة ومستهلَكة (وقود/تكلفة/TCO) لكنها متجاوَزة كمدخل | طبقة قديمة حيّة | DUP-01: تقييد الإنشاء اليدوي + إعادة تأطير «سجل تنفيذ» | مهم |
| CHECK-01 | `transport_intake_rules` | الجدول + 4 endpoints + صفحة rules-admin | السطح كامل؛ **تطبيق القواعد على مسار الإنشاء يحتاج تحققًا حيًا** (هل create يستشير القواعد فعلاً؟) | غير مؤكَّد | فحص حيّ ضمن E2E؛ إن ثبت عدم الاستهلاك → إمّا ربط فعلي أو أرشفة السطح | مهم (تحقّق أولاً) |
| CHECK-02 | `transport_itineraries` | جداول + 9 endpoints + صفحتان | السطح موجود؛ مدى الاستخدام الفعلي مقابل multi-leg bookings يحتاج بيانات إنتاج | غير مؤكَّد | قرار مالك بعد E2E: مساران متوازيان للتسلسل (lines داخل حجز ↔ itinerary عابر للحجوزات) — توثيق متى يُستخدم كلٌّ منهما | متوسط |

**ملاحظة منهجية:** البندان CHECK-* لا يُحسمان من القراءة الساكنة — مدرجان في خطة E2E (الملف 17) قبل أي قرار حذف، التزامًا بقاعدة «لا حذف بلا أثر واضح».
