# 02 — جرد الكيانات

> المصدر: فحص حيّ للـmigrations + `db/schema_pre.sql` + آلات الحالة في الراوترات على main (`d9266e54`).

---

## 1. كيانات خط الأنابيب الكنسي (Booking → Dispatch → Execution → Candidate)

| الجدول | الـmigration | الأعمدة المفصلية | آلة الحالة | soft-delete | القيود الفريدة |
|---|---|---|---|---|---|
| `transport_bookings` | 266 (+279 geo, +295 canon) | bookingNumber, customerId, **tripFamily**, **routePatternId**, نوافذ زمنية, اتفاق العميل (requestedVehicleClass/substitutionPolicy) | 10 حالات: draft→submitted→pending_approval→approved→scheduled→dispatched→in_progress→completed (+cancelled/rejected) | ✅ | (companyId, bookingNumber) |
| `transport_booking_lines` | 266 (+292 geo) | bookingId, lineNumber, requiredVehicleType/CapacityKg/SeatCount/LicenseClass | open→dispatched→in_progress→completed (+cancelled) | ✅ | (bookingId, lineNumber) |
| `transport_dispatch_orders` | 266 | vehicleId, driverId, scheduledStart/EndAt, linkedManifestId, linkedTripId, estimatedDistanceKm | 8 حالات: pending→notified→accepted→executing→completed→closed (+declined/cancelled) | — | (bookingId, lineId, vehicleId, driverId) |
| `transport_billing_candidates` | 261 | sourceType, sourceId, serviceType, serviceDate, quantity+unit, suggestedRevenue/Cost, materializedJournalEntryId | pending→materialized / rejected | — | **(companyId, sourceType, sourceId)** — مفتاح العصمة |
| `transport_route_patterns` | 295 | patternCode, **daysOfWeekMask** (7-bit), departureTime, activeFrom/Until, defaults, operationalWaypoints (jsonb) | active / paused / archived | ✅ | (companyId, patternCode) |

## 2. كيانات الحمولة

| الجدول | الـmigration | المفصلي | آلة الحالة |
|---|---|---|---|
| `cargo_manifests` | 243 (+263 lifecycle) | manifestNumber, customerId (إلزامي عند الإنشاء), fleetTripId, totalWeight, freightRevenue/Cost, billingStatus, transportServiceType | **15 حالة**: draft→requested→approved→assigned_to_driver→driver_accepted→trip_started→arrived_pickup→loaded→in_transit→arrived_delivery→delivered→completed→ready_for_invoice→financially_closed (+cancelled). السائق يقف عند delivered. |
| `cargo_items` | 243 | description, quantity, weight, declaredValue, isHazmat+class | — |
| `cargo_manifest_checkpoints` | 305 | checkpointType (10 قيم مقيَّدة CHECK), measuredValue+Unit, lat/lng, recordedBy/At | **لا soft-delete عمدًا** — حقائق تدقيق |

## 3. كيانات التخطيط والملاحة (migration 271)

| الجدول | المفصلي | آلة الحالة |
|---|---|---|
| `transport_planning_settings` | صف واحد لكل شركة: mapProvider (manual_only/google_maps/mapbox/here_maps), mapProviderApiKey, restHours, buffers | — |
| `driver_navigation_sessions` | dispatchOrderId, lastLat/Lng/Speed/Heading | active→arrived_pickup→loaded→arrived_dropoff→delivered→ended |
| `transport_itineraries` + `_legs` | برنامج متسلسل، legType, assignedVehicle/Driver لكل مقطع | itinerary: draft→scheduled→in_progress→completed؛ leg: pending→…→completed (+skipped) |
| `transport_route_estimates` | cache تقديرات المسافة (TTL يوم، fuzzy ±0.0001) | — |
| `vehicle_location_snapshots` | لقطات GPS عالية الحجم | — (بيانات عابرة، لا soft-delete) |

## 4. كيانات التسعير والقواعد

| الجدول | المفصلي |
|---|---|
| `transport_price_rules` | customerId, serviceType, vehicleType, routeFrom/To, unitOfMeasure, unitPrice, minimumCharge, validFrom/To, priority |
| `transport_service_lines` | sourceType/Id, quantity, unitPrice, lineTotal, billingStatus (ready_for_accounting→invoiced), invoiceId |
| `transport_intake_rules` | operationType, defaults (costCenter, approval, billing flags) |

## 5. كيانات الأسطول المرجعية

| الجدول | المفصلي للنقل | آلة الحالة |
|---|---|---|
| `fleet_vehicles` | payloadKg + **operationalPayloadKg** + seatCount + **validForPassengers/validForCargo** (295) + أبعاد الصندوق/المحاور/الإطارات (262) + requiredLicenseClass | available/in_use/maintenance/inspection/reserved/decommissioned |
| `fleet_drivers` | employeeId (جسر HR), licenseClass+Expiry, **driverServiceProfile** (cargo/umrah/passenger/rental/mixed), restHoursRequired, lastDutyEndedAt, هوية سعودية (280) | available/on_duty/off_duty/on_leave/suspended/inactive |
| `fleet_rental_contracts` | withDriver+driverId, dailyRate/weeklyRate/monthlyRate, securityDeposit, **handover***/**return*** (293), overageAmount | draft→active→completed (+cancelled) |
| `fleet_rental_payments` | dueDate, amount, paidAmount, journalEntryId | pending/partial/paid/overdue |
| `fleet_trips` | **طبقة legacy** — sourceKey, clientId, cost, distance | scheduled/in_progress/completed/cancelled (4 فقط) |
| `vehicle_capacity_overrides` / `driver_eligibility_overrides` | سجلّ استثناءات موثَّقة بسبب | — |

## 6. أعمدة الـCanon (تأكيد الوجود)

| العمود | الجدول | الـmigration | الغرض |
|---|---|---|---|
| `tripFamily` | transport_bookings | **295** | الفصل الرسمي ركاب/حمولة |
| `routePatternId` | transport_bookings | **295** | FK لقالب الحمولة المتكررة |
| `validForPassengers` / `validForCargo` | fleet_vehicles | **295** | فلتر محرك الإسناد |
| `operationalPayloadKg` | fleet_vehicles | **295** | الحمولة التشغيلية الآمنة (دون الحدّ الفني) |

## 7. الخط الزمني للـmigrations (النقل فقط — 27 ملفًا)

179 (deletedAt prevent.) · 185 (umrah pilgrims) · 193 (شراء المركبة) · 194 (alerts) · 196 (sourceKey) · 228–232 (telematics) · 243 (cargo + fuel-trip link) · 245 (tires) · 247 (rental) · 252 (updatedAt trips) · 253 (subsidiary accounts) · 261 (billing candidates) · 262 (الملف الفني للمركبة) · 263 (15-state cargo) · 264 (أهلية الرخصة) · 266 (bookings+dispatch) · 267 (umrah manifest) · 271 (محرك التخطيط) · 279 (geo header) · 280 (هوية السائق KSA) · 285 (alerts idx) · 292 (geo lines) · 293 (تفتيش التأجير) · 295 (الـcanon) · 305 (checkpoints).

## 8. مرشَّحو الازدواج (ثلاثة جداول «رحلة») — القرار في الملف 13

| الجدول | الدور الفعلي اليوم | من يكتب فيه |
|---|---|---|
| `transport_bookings` | طبقة الاستقبال والتخطيط (الكنسية) | transport-bookings.ts + route patterns + umrah integration |
| `cargo_manifests` | طبقة التنفيذ والفوترة للحمولة (15 حالة + أصناف) | cargo.ts + fleet.ts (driver advance) |
| `fleet_trips` | **legacy** — متتبّع تكلفة/مسافة بسيط (4 حالات)؛ يستهلكه الوقود/الصيانة للفوترة | fleet.ts (INSERT حيّ) |

**خلاصة:** الثلاثة ليست تكرارًا محضًا — bookings→dispatch→manifest خط أنابيب مرتَّب (الربط عبر `linkedManifestId`/`linkedTripId`)، لكن `fleet_trips` طبقة موازية قديمة ما زالت تُكتب — التوصية في 13 §2.
