# 14 — خطة الإصلاح والتنظيم

> الإصلاحات المحدودة (غير البنيوية — البنيوية في 13). مرتّبة دفعاتٍ قابلة للتحويل المباشر إلى Issues (الملف 18). كل بند PR صغير مستقل من main.

---

## الدفعة 1 — عاجل

| # | الإصلاح | النوع | الملفات المتوقعة | معيار القبول |
|---|---|---|---|---|
| FIX-01 | مرشّح محاسبة للركاب (تنفيذ RM-01 الحد الأدنى: دالة passenger + استدعاء من إغلاق الحجز) | ربط بمحرك | fleetEngine.ts, transport-bookings.ts, اختبار | إغلاق حجز ركاب → صف pending بكمية pax؛ idempotent |
| FIX-02 | cron تجسيد القوالب + مفتاح تعطيل + تصحيح التعليق (RM-05) | بناء ناقص + توثيق | cronScheduler.ts, planning_settings (عمود boolean — migration صغيرة), route-patterns.ts | قالب الغد يتجسّد آليًا 06:30؛ التعطيل يعمل؛ لا تكرار |
| FIX-03 | دمج حوار نقاط التشغيل للسائق (من الـstash بعد اعتماد هذا الجرد) | إكمال واجهة | cargo-checkpoint-dialog.tsx, me-driver.tsx, اختبار | A-04 قابل للإثبات من الواجهة |

## الدفعة 2 — مهم

| # | الإصلاح | النوع | معيار القبول |
|---|---|---|---|
| FIX-04 | صفحة SPA لإدارة Route Patterns (قائمة + إنشاء بقناع أيام مرئي + تجسيد نطاق) | إكمال واجهة | TA-GAP-03 مغلقة؛ المشغّل يدير القوالب بلا API يدوي |
| FIX-05 | تبويب «الملف الفني» قابل للتحرير في vehicle-detail (الـ19 حقلًا عبر PATCH القائم) | إكمال واجهة | TA-GAP-06 مغلقة |
| FIX-06 | قاموس الحالات الموحَّد (RM-03) + استبدال الخرائط المحلية في context-card / me-driver / rental-detail | توحيد | صفر fallback إنجليزي خام |
| FIX-07 | ختم lastDutyEndedAt من إنهاء dispatch المباشر | منطق | TA-GAP-07 مغلقة؛ حارس الراحة دقيق |
| FIX-08 | تفرّع شاشة الإنشاء خياران→أنواع (RM-04) | UX | TA-UX-01 مغلقة |
| FIX-09 | فصل صلاحية الاعتماد (تفاصيله في 15 §3) | صلاحيات | TA-PERM-01 مغلقة |

## الدفعة 3 — متوسط/تنظيف

| # | الإصلاح | معيار القبول |
|---|---|---|
| FIX-10 | إزالة trips-create نهائيًا (DEAD-01) بعد دورة سماح | لا ملف/route؛ اختبار محدَّث |
| FIX-11 | إخفاء mapbox/here من الإعدادات أو وسم «غير مفعّل» (DEAD-02) | لا اختيار لمزوّد غير عامل |
| FIX-12 | إضافة التأجير لـFleetTabsNav + تصحيح «قواعس» الإملائية | TA-UX-03/04 مغلقتان |
| FIX-13 | وسم تعديل حقول الربط (customerId/umrahGroupId/contractId) حدثًا حساسًا مميزًا في الـaudit | TA-SEC-02 مغلقة |
| FIX-14 | تقييد الإنشاء اليدوي لـfleet_trips من الواجهة (RM-02 مرحلة أ) | DUP-01 معيارها متحقق |
| FIX-15 | نص الموقع الحر = label فقط عند وجود locationId (تحقق ناعم + تلميح واجهة) | بقيّة TR-028 محسومة |

## §5 قواعد حاكمة لكل إصلاح
- فرع مستقل من main لكل FIX · PR صغير · guard أخضر بلا override · لا JE من أي شاشة نقل · إعادة استخدام المحركات والمكوّنات القائمة حصراً · اختبار يثبّت معيار القبول داخل نفس الـPR.

## ما لا يُنفَّذ في هذه الموجات
حذف fleet_trips أو ترحيل حقوله (RM-02 ج) · materialize التأجير · التفصيص الداخلي للفوترة · أي شيء يلمس دفتر الأستاذ — كلها رهن مرحلة الترحيل المالي وقرار المالك.
