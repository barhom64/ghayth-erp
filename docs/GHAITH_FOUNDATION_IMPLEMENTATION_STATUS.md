# حالة تنفيذ Ghaith Operating Foundation — IMPLEMENTATION_STATUS

> **النوع:** مصالحة التوثيق مع الكود المدموج — مصدر الحقيقة الواحد لحالة الفجوات.
> **التاريخ:** 2026-05-30 · **الأساس:** `main` @ `14c2c2b`
> **الغرض:** وثائق المراحل 1–7 كُتبت **قبل** تنفيذ الفجوات؛ ثم نُفِّذت ودُمجت. هذا الملف يمنع التوثيق من الكذب على حالته: لكل فجوة موثّقة، ما حالتها **الآن** في الكود، وأي PR أغلقها، والدليل.
> **القاعدة:** لا يُعاد بناء شيء — يُصالَح الموجود فقط (قاعدة #1418: لا تكرار).

---

## 1. الفجوات المُغلقة (مُنفَّذة ومدموجة)

| المعرّف | الوصف الأصلي | الحالة | PR | الدليل في الكود |
|---|---|---|---|---|
| **VIS-001** | مسارات العمرة بلا بوابة وحدة | ✅ مُغلق | #1424 | `App.tsx` → `...tagRoutes(umrahRoutes, "umrah")` + عنصر القائمة `module:"umrah"` |
| **VIS-002** | لا تفعيل جزئي (Feature/Subscription) | ✅ مُغلق | #1424 | `migration 236 company_feature_flags` + `AppContext.isFeatureEnabled` + بوابة `ModuleRoute`/`filterItems` + `disabledFeatures` في `/permissions/my` |
| **RBAC-001** | الدور النشط لا يُسجَّل في التدقيق | ✅ مُغلق | #1424 | `migration 235 audit_logs.active_role_key` + `auditMiddleware` يمرّر `activeRoleKey` + `eventListeners.logAudit` + `createAuditLog(activeRoleKey?)` |
| **FND-006** | تغطية تدقيق ناقصة (legal/governance/automation/marketing/store/bi) | ✅ مُغلق | #1424 | `auditMiddleware.ENTITY_MAP` + `ENTITY_TABLE_MAP` + `auditEntities` موسّعة |
| **DOC-VIOLATION** | تكرار مرفقات (`umrah_attachments`) | ✅ مُغلق (مع تصحيح) | #1433, #1442 | `migration 237` backfill إلى `documents`+`document_entity_links`؛ نقاط `/umrah/attachments` تقرأ/تكتب عبر `documents`. **تصحيح:** `employee_documents` جدول امتثال يُبقى (ليس تكرارًا) |
| **RBAC-002** | لا إنشاء سريع موحّد | ✅ مُغلق | #1446, #1450 | `POST /admin/onboard` (ذرّي) + واجهة `/admin/user-onboarding` |
| **RBAC-004** | لا Effective Viewer ولا Permission Explainer | ✅ مُغلق | #1446, #1450 | `GET /admin/users/:id/effective-permissions` + `POST /admin/permissions/explain` + الواجهة |
| **RBAC-003** | كتالوجا صلاحيات متوازيان | ✅ مُحتوى (لا إجراء) | — | `isKnownPermission()` يقبل الاثنين؛ الانحراف مُجَسَّر مسبقًا (انظر `RBAC_EXISTING_ASSETS_AUDIT`) |

---

## 2. الفجوات المتبقّية (موثّقة، تحتاج بيئة تشغيل/قرار منتج)

| المعرّف | الوصف | لماذا لم تُنفَّذ في الجلسة | المرجع |
|---|---|---|---|
| **VIS-003/005** | تفعيل `perm` من featureCatalog + حكم حالة السجل للأزرار | تطوير واجهة واسع يحتاج تحقق تشغيلي | `ACTION_VISIBILITY_RULES.md` |
| **VIS-004/006** | واسم نضج الصفحة + إخفاء غير الجاهز في الإنتاج | يحتاج قرار سياسة إصدار | `VISIBILITY_ENGINE_SPEC.md` |
| **DOC-DROP** | إسقاط جدول `umrah_attachments` القديم | بعد فترة إهلاك + تحقق staging | `DOCUMENT_SERVICE_CONTRACT.md` |
| **RBAC-UI-اكتمال** | ربط مؤلّف الأدوار/محلل التعارض كواجهات كاملة | الأساس منجز؛ التوسعة تحتاج تحقق UI | `ROLE_COMPOSER_SPEC.md`, `ROLE_CONFLICT_ANALYZER.md` |
| **JOURNEY-1** | واجهة تهيئة الشركة (حاجز) | يحتاج قرار منتج/تصميم | `OPERATING_JOURNEY_CATALOG.md` |
| **FLEET-FUEL** | عيب ازدواج عدّ الوقود عند إغلاق الرحلة | يحتاج تحقق تشغيلي | `OPERATING_JOURNEY_CATALOG.md` |
| **اختبارات الرحلات** | تنفيذ Playwright فعليًا للرحلات الثماني | يحتاج DB + تطبيق شغّال | `OPERATING_JOURNEY_ACCEPTANCE_TESTS.md` |

---

## 3. معايير القبول الـ13 (#1418 §خامسًا) — الحالة

| # | المعيار | الحالة |
|---|---|---|
| 1 | GHAITH_MASTER_OPERATING_BLUEPRINT | ✅ |
| 2 | جرد الموجود | ✅ |
| 3 | توثيق الكيانات/الملكية/القرار/الأثر | ✅ |
| 4 | الرحلات التشغيلية | ✅ موثّقة (تنفيذ الاختبارات متبقٍّ) |
| 5 | الظهور والإخفاء | ✅ موثّق + VIS-001/002 منفّذان |
| 6 | الخدمات المشتركة | ✅ 12 عقدًا |
| 7 | نموذج المستخدم متعدد الأدوار | ✅ موثّق + منفّذ (onboard/effective/explain + واجهة) |
| 8 | تجربة عربية لغير التقني | ✅ موثّقة (تحقق usability متبقٍّ) |
| 9 | اختبارات القبول | ✅ موثّقة (التنفيذ الفعلي متبقٍّ) |
| 10 | قرار استخدام/تطوير/دمج/إخفاء/حذف | ✅ في كل ملفات الجرد |
| 11 | لا تكرار | ✅ (التزام صارم؛ هذا الملف يمنع تكرار التوثيق) |
| 12 | لا كسر للموجود | ✅ (كل PR مرّ guard) |
| 13 | كل شيء مرتبط بفلسفة غيث | ✅ |

---

## 4. خريطة الـ PRs المدموجة

| PR | المرحلة/النطاق |
|---|---|
| #1424 | التوثيق الكامل (52 ملف، م1–7) + إصلاحات VIS-001/VIS-002/RBAC-001/FND-006 |
| #1433 | تصحيح DOC-VIOLATION (فحص المخطط) |
| #1442 | توحيد `umrah_attachments` في الوثائق المشتركة |
| #1446 | إكمال RBAC backend (onboard + effective-permissions + explain) |
| #1450 | واجهة مركز المستخدمين والأدوار (#1413 §18) |

---

## 5. القرار

- **العمل التوثيقي والتنفيذي للأساس مكتمل ومدموج.** المتبقّي (§2) إمّا تحقق تشغيلي على بيئة حيّة أو قرار منتج — لا توثيق إضافي.
- **هذا الملف هو مرجع الحالة الوحيد.** أي ادعاء "فجوة 🔴" في ملفات الجرد يُقرأ مقترنًا بهذا الجدول (§1) الذي يعكس حالة الكود الفعلية.
- **ممنوع** إعادة إنشاء أي من الملفات الـ52 — تُطوَّر عند الحاجة فقط.
</content>
