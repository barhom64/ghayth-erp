# تدقيق فجوات مالية غير مجدوَلة ولا محجوزة — 2026-06-14

> **تدقيق قراءة-فقط** (لا كود). الهدف: مشاكل مالية حقيقية **خارج** backlog #2230–#2252 والـPRs المفتوحة.
> الفرع: `claude/festive-cori-a9gwz4` · يكمّل تقريري #2252.

## المنهج
مسح: كل fallback لـ`resolveAccountCode(..., "CODE")`، الإدراج المباشر في `journal_lines`، حارس التوازن، وظائف cron للترحيلات الدورية، مسارات FX، وكنس «مُعتمد بلا ترحيل». تقاطُع كل نتيجة مع المهام المحجوزة لاستبعاد المعروف.

---

## 🔴 1) خلط حسابات fallback النظامي (2150 + 4910 + 2120) — أوسع من #2251

**11 غرضًا متمايزًا** يسقط على حساب `2150` الواحد عند غياب ربط صريح:
`cargo_freight_payable` · `commission_payable` · `fleet_fines_payable` · `fleet_trip_payable` · `leave_accrual_liability` · `legal_fee_payable` · `legal_payable` · `owner_payable` · `property_owner_payable` · `property_maintenance_payable` · `purchase_grni`.

- **الدليل:** مسح `resolveAccountCode(..., "2150")`؛ الحسم عبر `getAccountCodeFromMapping` ثم fallback (`financialEngine.ts:resolveAccountCode`)؛ **صفر صفوف seed في `account_mappings`** (بحث `INSERT INTO account_mappings` في `db/` + migrations = 0) ⇒ الـfallback نشط **افتراضيًّا في كل تركيب**.
- **الأثر:** الميزانية تعجز عن التمييز بين ديون مُلّاك العقارات/المحامين/الموردين (GRNI)/العمولات/غرامات الأسطول — كتلة واحدة. تعذُّر التقادم والتسوية بالطرف من دفتر الأستاذ.
- **لماذا غير محجوز:** #2251 يغطّي **2160 فقط**. الحارس `assertPostableAccount` يمنع الترحيل على حساب غير قابل لكنه **لا يمنع الخلط** (عدّة أغراض ← حساب واحد).
- **الخطورة:** 🔴 عالية (نشط افتراضيًّا، واسع، يضرب صدق الميزانية).
- **التوصية:** ربط purpose صريح لكل غرض (حساب فرعي مخصّص حيث يلزم: ذمم مُلّاك، GRNI، عمولات، غرامات)؛ + guard يفشل عند سقوط غرضين متمايزين على نفس الحساب العام. backfill تاريخي موقوف على إذن المالك.

### تعميم (تحليل شامل لكل fallback — جولة الغوص العميق):
الخلط **ليس على 2150 فقط** — حسابان آخران مؤكَّدان من نفس الفصيلة:
- **🟠 4910 «فوائد ومرابحات بنكية»:** يبتلع `fx_revaluation_gain` + `property_sale_gain` رغم وجود **4920 «أرباح بيع أصول» منفصل** (`asset_disposal_gain → 4920` الصحيح، بينما `property_sale_gain → 4910` خطأ). قائمة الدخل لا تميّز الفوائد عن ربح الصرف عن ربح بيع العقار.
- **🟠 2120 «مستحقات الرواتب»:** يبتلع `payroll_deductions_payable`/`employee_deductions` (مستقطَعات تُورَّد للغير) مع صافي الراتب — صافي ما يُدفع للموظف مخلوط بالمستقطَع (GOSI 2140 و WHT 2132 لهما حساباتهما، لكن الباقي catch-all على 2120).
- **استُبعد كشرعي (حسابات تحكّم حقيقية):** 1111 (نقد)، 1131 (ذمم مدينة control)، 1180 (ضريبة مدخلات)، 5260/5270 (مرادفات اسمية لنفس المصروف).
⇒ #2277 يُعمَّم: ليس «2150» بل **«ربط purpose صريح + guard منع الخلط»** يشمل 2150/4910/2120 (عُلّق على #2277).

## 🟠 2) إعادة تقييم العملات ليست مُؤتمَتة — تذكير لا محرّك

- المنطق كامل: `computeRevaluationLines` + `runPeriodEndRevaluation` يكتب `fx_revaluation_log` (`fx/revaluation.ts:198-206`)؛ endpoint ترحيل `POST /finance/gl-helpers/fx-revaluation/:id` (`finance-gl-helpers.ts:306`)؛ قائمة pending (`:139`).
- **لكن** وظائف cron لـFX: جلب يومي (`daily_fx_rate_fetch`) + تنبيه قِدَم (`fx_staleness_check`) + **تذكير CFO** (`monthly_fx_revaluation_reminder` يوم 28، `cronScheduler.ts:4625`). **لا وظيفة تُشغّل التقييم/الترحيل آليًّا.**
- **عيب مقترن:** نصّ التذكير يحيل إلى `‎/finance/fx/revaluation/post` بينما المسار الفعلي `‎/finance/gl-helpers/fx-revaluation/:id` — **رابط خاطئ في الإشعار**.
- **الأثر:** أرباح/خسائر صرف غير محققة على ذمم/موردين بعملة أجنبية مفتوحة لا تُرحَّل ما لم يتذكّر إنسان (مشروط بتعرّض غير-SAR — وارد في العمرة/الموردين الأجانب).
- **شقيق (FX المحقَّق عند السداد):** `recordRealizedFx`/`postRealizedFxJournal` موجود لكن يُستدعى **فقط** من endpoint يدوي (`finance-gl-helpers.ts:351`)؛ مسارا السداد `‎/invoices/:id/payment` (`finance-invoices.ts:1911`) و`‎/customer-receipts` (`:4169`) **لا يحسبان فرق الصرف** بين تاريخ الفاتورة والسداد ⇒ المكسب/الخسارة المحقَّقة لا تُرحَّل آليًّا. نفس جذر #2278 (أدوات FX يدوية غير مدموجة بالتشغيل) — وُسّع #2278 ليغطّي الشقّين.
- **الخطورة:** 🟠 متوسطة (مشروطة بالتعرّض). **غير موجود في #2230.**
- **التوصية:** أتمتة كـprofile على `FIN-RECURRING-POSTING-ENGINE` (نمط `monthly_auto_depreciation`) + بوّابة الإقفال + إصلاح رابط التذكير.

## 🟡 3) نمط نظامي: «تذكير/يدوي بدل محرّك» في ترحيلات نهاية الفترة

| الترحيل الدوري | الحالة |
|----------------|--------|
| إهلاك الأصول | ✅ مُؤتمَت (`monthly_auto_depreciation`, `cronScheduler.ts:4590`) |
| استحقاق نهاية الخدمة/الإجازات | ❌ endpoint يدوي (`hr.ts:7484`) — راجع تقرير #2252 |
| إعادة تقييم العملات | ❌ تذكير فقط (البند 2 → #2278) |
| تسوية الضرائب | ❌ بلا مسار (البند 5 → #2280) |
| مخصص الديون المشكوك فيها | ⚠️ endpoint ترحيل موجود (`/finance/bad-debt/post`, `finance-invoices.ts:3940`) لكن **بلا أتمتة مدفوعة بالتقادم** — تذكير CFO فقط (`monthly_bad_debt_reminder`, `:4624`) |
| تحضير الإقفال | ⚠️ تذكير (`monthly_closing_prep`, `:4575`) |

صدق الدفتر كل فترة يعتمد على بشر يتذكّرون. الوحيد المُؤتمَت هو الإهلاك؛ البقية (استحقاقات HR، FX، الضرائب، مخصص الديون) يدوية/تذكير. #2247/#2248/#2250 ستؤتمت المؤجّل/الإطفاء/الإقفال — لكن **FX (#2278) والضرائب (#2280) وجدولة استحقاقات HR ومخصص الديون قد تسقط بين المهام**. التوصية: `FIN-RECURRING-POSTING-ENGINE` يبتلعها كـprofiles. (مخصص الديون أنعم — القدرة موجودة، تنقصها الأتمتة فقط؛ لم يُفتح له issue منفصل.)

## ⚪ 4) دوال ترحيل ميتة ومتباعدة (ثانوي)

`postLeaveAccrualGL` (`hrEngine.ts:208` — يستخدم 2150 الصحيح للإجازات) و`postEOSAccrualGL` (`:236`) **غير مستخدمتين**؛ المسار الحيّ `postMonthlyAccrualsGL` (`:521` — يستخدم 2220 الخاطئ لالتزام الإجازات). الدالة الميتة «أصحّ». **التوصية:** حذف الميت + توحيد على المسار الصحيح (يتقاطع مع إصلاح #2252-#3).

---

## 🟠 5) لا محرّك تسوية/سداد ضريبي يُصفّي 2131/2132/2133 — عائلة #2278

ضريبة المخرجات (2131) والاستقطاع (2132) والزكاة (2133) تُستحَق على المعاملات، لكن لا ترحيل تسوية يُصفّيها عند السداد للهيئة.
- **موجود:** استحقاق (`CR 2131/2132`)، عكس على المرتجعات (`DR 2131` — `finance-invoices.ts:2700,2801,3221`)، تقرير انحراف ما قبل الإقرار (`finance-reports.ts:2985+`)، فوترة ZATCA الإلكترونية (`finance-zatca.ts` — إرسال فقط، بلا ترحيل GL).
- **مفقود:** purpose سداد ضريبي، جدول `vat_returns`/`tax_filings`، endpoint/cron تسوية، وأي تصفية للزكاة 2133 إطلاقًا.
- **الأثر:** التصفية تعتمد على قيد يدوي وإلا تراكمت الأرصدة. **الخطورة:** 🟠 متوسطة. **غير محجوز.** → **#2280**.

---

## 🔄 تحديث مزامنة (2026-06-14): هبوط #2233 (عقد البُعد)

اندمج `6016330` (#2233): `assertDimensionContract` يُفرَض عند باب الترحيل (`gl/posting.ts:163+` + `financePostingPolicy.ts:67+`)، ratchet (enforce لوقود المركبة 5510 فقط، warn للبقية).
- **ملاحظة مهمّة:** العقد يغطّي أبعاد **vehicle/property/project/vendor/client فقط** — **لا employee/department/branch**. فأبعاد EOSB/الرواتب (راجع #2252-#2) **خارج العقد**؛ يحتاج توسيع فئة أبعاد العمالة. (ملك #2246 للمطور — موثّق لا مفتوح، تفاديًا للتصادم.)
- نتائجي الأخرى تبقى صالحة: #2277 (خلط الحساب) و#2280 (الضريبة) **متعامدان** على العقد (يفرض الأبعاد لا صحّة الحساب). حارس التوازن انتقل إلى `gl/posting.ts:86`.

---

## 🔬 جولة المسح العميق (2026-06-14) — 6 زوايا سلامة دفترية

مُتحقَّقة يدويًّا بعد مسح وكيل. (مناطق 1/6 سليمة، 2/3 فُتحتا، 4/5 موثّقتان.)

- **🔴 (2) الفوترة متعددة العملات — `#2295`:** الترحيل لا يحوّل للعملة الوظيفية ولا يلتقط السعر؛ أعمدة العملة (migrations 076/171) لا تُملأ؛ `fx/convert`+`rate-lookup` غير موصولين ⇒ النظام أحادي العملة فعليًّا، و realized-FX (#2278) يحسب صفرًا. **جذر منفصل عن #2278.**
- **🟠 (3) الموازنة على قناة الشراء — `#2296`:** المصروف اليدوي يُحظر عند 110% (`finance-journal:981`)، لكن PO/GRN/الفاتورة تستهلك/تُبلّغ بلا منع قبلي ولا حجز (`finance-purchase:1651,3351`).
- **🟠 (4) ابتلاع GL صامت في umrah — موثّق، لم يُفتح:** 5 مواقع تعتمد المستند ثم تبتلع فشل `postGL` في `catch(glErr)` فتُعيد 200 مع `journalEntryId=null` (`umrah.ts:2390,2618,3449` + waiver + `eventListeners.ts:1467`). **مخفّف بقوة:** كلها تُسجَّل في `financial_posting_failures` (`businessHelpers.ts:835`) ويستنزفها نظام إعادة (`postingFailureRetry.ts` + `finance-hardening.ts`). يبقى الخلل: نجاح ظاهري + ترحيل لا-ذرّي بعد الـcommit حتى يُعيد admin الطابور. (مرشّح issue إن أردت — لم أفتحه لأنه مخفّف.)
- **⚪ (5) إعادة تصنيف عمولة الرواتب — uncertain، داخل حدود #2252:** قيد reclass يستخدم `(payroll_runs, runId)` بلا sourceKey صريح فيصطدم بمفتاح قيد الاستحقاق نفسه (`eventListeners.ts:1045` مقابل `hrEngine.ts:473`) ⇒ احتمال **تخطّي** (لا ازدواج) عند `comm>0`. ضمن مراجعة #2252.
- **✅ (1) الأرصدة الافتتاحية:** حارس DR=CR مزدوج (route `finance-journal:3294` + engine `businessHelpers:625`) — لا إدخال غير متوازن.
- **✅ (6) حاجز الفترة المُقفلة:** `checkFinancialPeriodOpen` على تاريخ المستند الفعلي في كل المسارات (30+ موقعًا)؛ التجاوز الوحيد إقفال نهاية السنة المقيَّد بـ`type:'closing'`.

---

## 🧾 امتداد HR/الرواتب (2026-06-14) — سلامة دفترية

طُبِّق نفس المنهج على ترحيل الرواتب (غير متصادم: المطور على finance-core/#2079/#2080).

- **🔴 عنقود الرواتب — `#2303`:** قيد الراتب (`mudad/post-salary-journal.ts:2-4`) يجمع كل الاستقطاعات في `salary_deductions_payable` واحد **لا يُخصَم أبدًا** (يتراكم بلا تمييز/تسوية)؛ ذمة السلفة **1141 لا تُسحب** عند الاسترداد (`postLoanDisbursementGL` موجود، لا `postLoanRepaymentGL`؛ الاسترداد يُتتبَّع في `hr_loan_installments` لكن GL لا يُقيَّد) ⇒ تباعد GL↔subledger؛ GOSI **2140** بلا تسوية دفترية (سيبلنغ #2280).
- **🟠 خلط `settlement_payable → 2140`** (`hrEngine.ts:151`): تسوية نهاية الخدمة على حساب GOSI — أُضيف لـ#2277 (تعليق).
- **✅ WPS:** موصول (`hr-wps.ts` + `wps/builder|parser|run` + مدد) — مستبعَد.

---

## 🧮 فحص منهجي للدفتر (2026-06-14) — حسابات أب + التزامات دائنة-فقط

تحليل آليّ لكل `resolveAccountCode` fallback:

- **🔴 fallback لحساب أب غير قابل للترحيل — `#2325`:** 6 أغراض تقع افتراضًا على حساب أب (`allowPosting:false`) فتُرمى عند `assertPostableAccount`: `property_owner_receivable→1140` · `vat_output→2200` (يجب 2131) · `umrah_commission→5200` · `umrah_transport_expense→5300` · `umrah_transport_payable→2100` · `asset_cost→1200`. على التركيب الافتراضي (صفر mappings) ⇒ المسار يفشل (وفي umrah صامتًا/#2301). فئة فشل صلب، مختلفة عن #2277.
- **التزامات دائنة-فقط (تأكيد منهجي لـ#2280/#2303/#2252):** `2132 wht_payable` · `2140 gosi/settlement` · `2220 eos/leave` تُقيَّد دائنًا فقط (لا debit) ⇒ تتراكم بلا تسوية — يطابق فجوات التسوية المرصودة.
- **استُبعد:** `2170 security_deposit` ظهر «دائن-فقط» لكنه **يُردّ فعلًا** (ternary في `postSecurityDepositGL`) — إيجابي كاذب، مستبعَد.

---

## استُبعد (سليم)
- ✅ حارس التوازن DR=CR مُنفَّذ (`gl/posting.ts:86` يرمي عند فرق >0.01).
- ✅ الإدراج المباشر في `journal_lines` هو المرحِّل الكنسي (`posting.ts`/`financialEngine.ts:259`) لا تجاوز.
- ✅ `property_maintenance_payable → 2160` ضمن نطاق #2251 (محجوز).
- ✅ تصفية GRNI: لها مسار فعلي (three-way match + obligation، `finance-purchase.ts:1629`) — لكن على 2150 المخلوط (مغطّى بـ#2277).
- ✅ التسوية البنكية: لها بنية (تعديلات FIN-18 `businessHelpers.ts:1438`، دفعات `BANK`، حدث `finance.reconciliation.completed`).
- ✅ عكس القيود: `reverseAccountBalances` عام موجود (`businessHelpers.ts:867`)؛ سحب مخصص EOSB مغطّى بـ#2252.
- ✅ **اكتمال الترحيل (approved-but-unposted):** طبقة رصد ناضجة موجودة — `GET /reports/gl-integrity-gaps` (`finance-reports.ts:3171`: فواتير/credit/debit memos/payment_runs منفَّذة بلا journalId + spa_orphans) + `/reports/unmapped-lines` + `/reports/operation-gaps`. **ملاحظة خفيفة:** تقارير سلبية (لا cron/تنبيه يستهلكها) — يُفترض أن تستهلكها بوّابة الإقفال #2250 (محجوزة). مرصود ومغطّى.

---

# ملحق — issues مفتوحة من هذا التدقيق

> ✅ فُتحت (8): **#2277** (خلط الحسابات) · **#2278** (FX) · **#2280** (تسوية ضريبية) · **#2295** (متعددة العملات) · **#2296** (موازنة الشراء) · **#2301** (ابتلاع GL في umrah) · **#2303** (عنقود الرواتب) · **#2325** (fallback لحساب أب غير قابل) — كلها مربوطة بالأم #2230.

## #2277 — البند 1
**العنوان:** `FIN-FIX-2150-CONFLATION: أغراض متمايزة تسقط على حساب 2150 العام`
**الأم:** #2230

> ## المشكلة
> 11 غرضًا محاسبيًّا متمايزًا (`cargo_freight_payable`, `commission_payable`, `fleet_fines_payable`, `fleet_trip_payable`, `leave_accrual_liability`, `legal_fee_payable`, `legal_payable`, `owner_payable`, `property_owner_payable`, `property_maintenance_payable`, `purchase_grni`) يسقط على حساب `2150` الواحد عند غياب ربط صريح. ولا توجد صفوف seed في `account_mappings` ⇒ الخلط نشط افتراضيًّا. يضرب صدق الميزانية ويمنع التقادم/التسوية بالطرف.
>
> ## المرحلة 1 — Audit
> لكل غرض: ما الحساب الصحيح؟ (حساب فرعي مخصّص: ذمم مُلّاك، GRNI، عمولات، غرامات...) — في وصف الـPR.
> ## المرحلة 2 — إصلاح محدود
> ربط purpose صريح لكل غرض عبر `account_mappings`/الـseed؛ + guard يفشل عند سقوط غرضين متمايزين على نفس الحساب العام (نظير lint الموجود). لا backfill تاريخي بلا إذن المالك.
> ## اختبارات
> assertion على `journal_lines`: كل غرض يُرحَّل على حسابه المخصّص لا 2150 العام.
> ## خارج النطاق
> تصحيح أرصدة تاريخية دون تفويض · تغيير شجرة الحسابات الكاملة.
> ## القبول
> لا غرضان متمايزان يتشاركان حسابًا عامًّا · guard أخضر · اختبارات تثبت الفصل.

## #2278 — البند 2
**العنوان:** `FIN-FX-REVALUATION-AUTOMATION: أتمتة إعادة تقييم العملات (محرّك بدل تذكير)`
**الأم:** #2230

> ## المشكلة
> منطق إعادة التقييم كامل (`computeRevaluationLines`/`runPeriodEndRevaluation` → `fx_revaluation_log`؛ endpoint `POST /finance/gl-helpers/fx-revaluation/:id`)، لكن الترحيل **يدوي** — وظيفة cron الوحيدة الشهرية هي تذكير CFO (`monthly_fx_revaluation_reminder`). إضافةً: نصّ التذكير يحيل إلى رابط خاطئ (`/finance/fx/revaluation/post`).
>
> ## المرحلة 1 — تحقّق
> هل `runPeriodEndRevaluation` يُحسب دوريًّا؟ كم سجلّ pending متراكم؟ ما تعرّض غير-SAR الحالي؟
> ## المرحلة 2 — أتمتة (profile على المحرك الدوري)
> تشغيل التقييم + ترحيل آلي شهري (نمط `monthly_auto_depreciation`) مع بوّابة الإقفال + idempotency؛ + إصلاح رابط التذكير ليطابق المسار الفعلي.
> ## اختبارات
> assertion على `journal_lines`: قيد إعادة تقييم متوازن (DR/CR على `fx_revaluation_gain/loss` + `ar/ap`) لا يتكرّر لنفس الفترة.
> ## خارج النطاق
> تغيير مصادر الأسعار · backfill تاريخي بلا إذن.
> ## القبول
> إعادة التقييم تُرحَّل آليًّا للفترات ذات التعرّض · رابط التذكير صحيح · guard أخضر.
