# 08 — جرد التقارير المالية (الموجة 3)

## المصادر
`routes/finance-reports.ts` · `routes/bi.ts` · `exec-dashboard` · تقارير المجالات (لكل بُعد).

## منهجية لكل تقرير
المصدر (يجب: `journal_lines` الموسومة/القيود — لا جداول ظل) · الفترة (`check:finance-period-drift` ناشط) · الفرع/الشركة scoped · مكرر مع؟ · ضعيف/بطيء؟ · مستخدم فعليًا (تتبع apiFetch من الواجهة عبر audit:wiring)؟

## ملاحظة حصر أولية
`audit:wiring` يكشف 36 endpoint مالية بلا مستهلك واجهة (dso-trend، customer-360، fixed-assets transfer/dispose/impair/revalue، CIP، …) — مادة الموجة 3: إما توصيل أو أرشفة بقرار، **لا حذف بلا إثبات موت**.
