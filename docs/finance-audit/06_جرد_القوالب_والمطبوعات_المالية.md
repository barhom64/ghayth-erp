# 06 — جرد القوالب والمطبوعات المالية (الموجة 1)

## المصادر
`lib/print/templateResolver.ts` (presets) · `print/brandedThemes.ts` · `print/archive.ts` · `routes/print.ts` · هجرة 172 (بذور القوالب) · `pages/.../print*`.

## القوالب الاثنا عشر المستهدفة بالتوحيد (من #2083)
فاتورة مبيعات · فاتورة مشتريات · سند قبض · سند صرف · قيد يومية · كشف حساب عميل · كشف حساب مورد · تقرير ضريبي · تقرير مراكز تكلفة · تقرير أصل · تقرير عهدة · إشعار دفع/تحصيل.

## منهجية لكل قالب
الغرض · المصدر الذي يسحب منه (يجب: المستند الحقيقي + القيد، لا جداول جانبية) · هل عبر الطباعة الموحدة؟ · المكررات (لا يبقى قالبان لغرض واحد إلا بسبب تشغيلي مكتوب) · الحالة (قديم/حي) · معيار القبول: PDF ناتج يطابق القيد المرحّل.

## ملاحظات أولية
- `receipt_voucher` preset موجود (templateResolver:272) — يُفحص ربطه بسند القبض الجديد (FA-003) بدل أي مصدر قديم.
- مخطط ترقيم `finance/receipt_voucher` مُعطَّل في numbering (هجرة 229) — قرار إحياءه يتبع توحيد القالب.
