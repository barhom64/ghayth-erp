# 09 — جرد Finance Intake والتكاملات (الموجة 4 — FIN-027 حرج)

## المبدأ (القاعدة 5)
المسارات الأخرى لا تكتب قيدًا — ترسل **مرشح ترحيل/سياقًا** والمحرك المالي يصنّف (operation/domain/scenario) وينتج نفس المعاينة/الترحيل (R-006).

## القنوات المستهدفة بالحصر
| القناة | الحالة المبدئية |
|---|---|
| النقل → Accounting Candidates | قائم جزئيًا (rental close handoff #2026؛ مرشحات الحمولة) — **فاتورة 1+N** (مهمة 9) |
| العقار (إيجارات/صيانة/ملاك) | عبر propertiesEngine — يُجرد التزام القاعدة 5 |
| العمرة (فواتير/عمولات/إعادة تصنيف) | محركات قائمة — عمولة HR وصلت payroll (#2058) |
| المستودع/المخزون (GRN/COGS/جرد) | COGS عند الاعتماد ✓ · GRN treatment ⚠️ (مهمة 1) |
| HR (رواتب/سلف/عهد/عمولات) | gl mappings مبذورة (256) — يُجرد التوازن حيًّا |
| Excel/API استيراد | `bank_statements` import ✓ · generic_import — يُجرد التصنيف |

## مخرج الموجة 4
مصفوفة: قناة → نقطة دخول → مصنِّف → معاينة → قيد → حالة ثلاثية → Audit، مع فجوة/مهمة لكل خانة ناقصة.
