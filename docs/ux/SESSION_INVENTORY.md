# جرد الجلسة — SESSION_INVENTORY

> سجلٌ منظَّم لكل ما نُفِّذ في جلسة حوكمة التنقّل + تنظيف الإعدادات + الحماية.
> الفرع: `claude/vigilant-pasteur-mje1sm` · **56 commit** · كله مدفوع، الشجرة نظيفة.

---

## أ) موجة حوكمة التنقّل — 12 شريحة
تقرير الجرد (`NAVIGATION_DUPLICATE_INVENTORY.md`) · المعجم العربي + `navigation.canonical-map.ts` · حارسا التبويبات والإجراءات السريعة · تعريب التسميات الإنجليزية · توحيد عنقود `/bi` لمدخل واحد · تمييز التصادمات الاسمية · قرارات المالك (audit-log/الموردون) · توحيد `work-inbox`/`hr-services` · ربط البوّابات في `guard.sh` · جعل الـaliases قابلة للبحث في command palette · تعريب «ZATCA Reports Hub» + dedup دليل العقارات.

## ب) إعادة تنظيم المالية + تدقيقات
`FINANCE_INVENTORY_AND_ORGANIZATION.md` · **تقسيم مجموعتين ضخمتين** («الضرائب والتقارير» 34 مدخلًا، «الحسابات والقيود» 20) · تمييز تقادم الذمم · `CROSS_MODULE_DUPLICATION_AUDIT.md` (مع تصحيح ادّعاءات تكرار مبالَغة) · dedup «عمليات متقدّمة» بالمستودع · ربط `gate:nav`.

## ج) بناء 8 واجهات مالية مخفية (ميزات فعلية)
خدمات لها backend بلا UI — بُنيت واجهاتها:
1. الإطفاء المقدّم (prepaid-amortization) · 2. الإيراد المؤجل (deferred-revenue) · 3. طابور فشل الحسابات الفرعية · 4. datafix أبوّة الحسابات · 5. الإنشاءات تحت التنفيذ CIP (+ دورة حياة كاملة) · 6. مركز التصنيف المحاسبي (+ ربط وتصنيف) · 7. أقساط التأمين (+ منتقيات الكيانات) · 8. مطابقة PO الثلاثية → إجراءات الدفع.

## د) دمج الإعدادات
ربط الـ15 تبويبًا بمسارات مباشرة · قفزة الفترات المالية · **دمج توجيه الإشعارات** (صفحتان→واحدة، نفس الجدول) · **تقاعد الهيكل B الموازي** (`/org/*`) · إخفاء بطاقات الاعتماد الميتة (واتساب/VAPID/ZATCA) · إسقاط «سير العمل» (جدول بلا مُنفِّذ) · تعنقُد التبويبات الـ15 · إصلاح module-dashboards-403 · dedup مُركّب الأدوار.

## هـ) إصلاحات وظيفية
🔴 **خطأ مفاتيح chainType** — سلاسل موافقات الإعدادات كانت تُكتب بمفاتيح (`leave`) لا يقرؤها المُحلِّل (`leaves`) فتُهمَل **صامتةً**؛ صُحِّحت لتُطبَّق فعلًا · **دمج صفحة استحقاقات HR** المكرّرة · إزالة تبويب «الإجراءات» الخامل (مُتجاوَز بالموافقات) · تمييز الحوكمة/السياسات/المخاطر/الامتثال · تسوية السجل مع canonical-map · **bad-debt** صُحِّح من «الديون المعدومة» إلى «مخصّص الديون المشكوك فيها» (الصفحة مخصّص لا شطب).

## و) اتّساق القوائم — موجتان
**24 تناقضًا مُضلِّلًا/مرادفًا** أُصلح عبر الشريط + التبويبات + الصفحات: purchase-orders «طلبات»→«أوامر» · event-monitor «مراقبة»→«كتالوج» · VAT readiness · التوجيه البُعدي · الأعذار→الاستئذان · المخاطر/الامتثال المُميَّزة …

## ز) تعريب العناوين + الحماية الدائمة
**26 عنوان صفحة** عُرِّب (حُذفت اللواحق الإنجليزية: CFO Cockpit، Approvals Inbox، Reconciliation Hub…) مع إبقاء الاختصارات الراسخة · **حارسان جديدان**: `gate:labels` (تكرار/لاتيني/انحراف بالشريط) + `gate:nav-titles` (لاتيني بعناوين الصفحات).

---

## الأثر الإجمالي
- **8 ميزات مالية** جديدة (واجهات لخدمات كانت مخفية).
- **~50 إصلاح** تنقّل/تسمية/اتّساق عبر الأنواع الثلاثة.
- **خطأ بيانات حقيقي** اكتُشف وأُصلح (سلاسل الموافقات الصامتة).
- **حارسان دائمان** يمنعان عودة التكرار/اللاتيني/الانحراف.
- **5 بوّابات تنقّل خضراء** · 0 orphan · 0 dead-link · typecheck=0.

> تفاصيل القوائم الثلاث الحالية: `docs/ux/MENU_INVENTORY.md`.
