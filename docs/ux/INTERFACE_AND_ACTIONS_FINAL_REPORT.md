# التقرير النهائي — مراجعة تخطيط الواجهة وديناميكية الإجراءات

- **التاريخ:** 2026-06-23
- **النطاق:** مراجعة شاملة (تخطيط + تنقل + هندسة معلومات + نظام تصميم + ديناميكية الأزرار) + بوابة إنفاذ.
- **الأساس:** مسح فعلي للكود عبر 4 مساحات استكشاف (لا ملخّصات). الخطة: `plans/interface-actions-review-2026-06-23.md`.

## 1. الحكم العام

نظام غيث **ناضج في طبقات التنقل والإجراءات الخطرة ونظام التصميم الأساسي**. لا توجد عيوب P0/P1 معمارية في هذه الطبقات. التحسينات المتبقية محصورة، موثّقة، وأغلبها قرار مالك أو دفعات تجميلية لا تكسر شيئًا.

| الطبقة | الحالة | عيوب P0/P1 |
|---|---|---|
| التنقل وهندسة المعلومات | ناضج، 6 حُرّاس خضراء | لا يوجد |
| الإجراءات الخطرة | محكم (GuardedButton ~1750، dialogs موحّدة) | لا يوجد |
| نظام التصميم | أساس موحّد، تطبيق متفاوت | لا يوجد |
| الأزرار الميتة/Microinteractions | نظيف (A-) | لا يوجد |

## 2. ما أُنجز في هذه الجولة (مراجعة + إنفاذ)

1. **وثائق المراجعة الأربع:**
   - `INTERFACE_LAYOUT_REVIEW.md` — التخطيط والتنقل وهندسة المعلومات.
   - `DYNAMIC_ACTIONS_MATRIX.md` — مصفوفة الإجراءات الخطرة × (صلاحية/حالة/تأكيد/أثر).
   - `DESIGN_SYSTEM_GAPS.md` — جرد نظام التصميم وفجواته.
   - هذا التقرير النهائي.
2. **حارس ثابت:** `scripts/src/check-dangerous-actions.mjs` (+ اختبار وحدة + baseline 38 موضعًا) — يمنع نمو نمط `confirm()` الأصلي للإجراءات الخطرة. مربوط في `guard.sh` و`package.json` (`check:dangerous-actions`)، يبدأ أخضر.
3. **اختبار Playwright للأزرار الحرجة:** `e2e/tests/ux-critical-actions.spec.ts` (`@ux-gate`) — يفشل عند تسرّب نافذة متصفّح أصلية أو وجود أزرار ميتة بلا اسم وصول.

## 3. الصفحات/البنود التي تحتاج إعادة ترتيب أو تحسين (مجدولة دفعات)

| البند | الموضع | المستوى | المالك | القرار |
|---|---|---|---|---|
| إغلاق الموسم بزر خام بلا صلاحية/تأكيد | `pages/umrah/seasons.tsx:63` | P2 | العمرة | لفّ بـ `GuardedButton` + `ConfirmActionDialog` |
| ~38 موضع `confirm()` أصلي | متعدّد (admin/hr/finance…) | P2 | كل مسار مالك | تحويل لـ dialog موحّد (محروس ضد النمو) |
| أزرار معطّلة بلا `title` | `hr/documents.tsx:472`, `fleet/transport-dispatch.tsx:288`, `project-detail.tsx:1003` | P3 | المسارات | إضافة سبب التعطيل |
| توحيد رسائل الخطأ (655+) | منتشر | P2 | عام ثم مسارات | معجم موحّد + دفعات |
| ألوان مضمّنة (1,279+) → رموز حالة | `shared/*timeline*` وغيرها | P2 | عام | حارس lint + دفعات |
| `<button>` خام → `Button` | login/entity-documents/bulk-actions… | P2 | عام | دفعة موحّدة |
| أسماء متعددة لنفس المسار | `/work-inbox`,`/my-space`,`/hr/services`,`/bi` | P2 | — | **قرار مالك** (اسم رسمي في canonical-map) |
| تجميع فرعي لمسارات المالية (207) | المالية | P2 | المالية | **قرار مالك** (تحسين) |

## 4. التحقق

| الفحص | النتيجة |
|---|---|
| `node scripts/src/check-dangerous-actions.mjs` | ✅ 38 baseline · 0 جديد |
| `node --test scripts/src/check-dangerous-actions.test.mjs` | ✅ 7/7 |
| `node scripts/src/check-e2e-login-pattern.mjs` | ✅ |
| `node scripts/src/ux-scorecard.mjs` | ✅ |
| اختبار e2e `ux-critical-actions.spec.ts` | يعمل ضمن مسار `e2e` (غير مُتحقَّق محليًا — لا تطبيق حيّ في البيئة) |

## 5. قرارات مطلوبة من إبراهيم

1. اعتماد أسماء رسمية موحّدة للمسارات متعددة الأسماء (قرار مالك، لا كود).
2. هل تُرشَّح `check:dangerous-actions` تلقائيًا ضمن `guard` (مربوطة فعلًا) — للعلم لا للقرار.
3. أولوية دفعات الإصلاح اللاحقة (umrah/seasons، تحويل confirm، توحيد الرسائل).

## 6. قاعدة القبول (مطبّقة)
لا يُعتمد مسار إذا كانت أزراره لا تعكس الحالة أو الصلاحية أو الأثر. الحارس الثابت + اختبار `@ux-gate` يحرسان هذا من الآن ويمنعان الارتداد.
