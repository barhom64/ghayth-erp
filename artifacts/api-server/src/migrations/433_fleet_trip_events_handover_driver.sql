-- ===========================================================================
-- 433_fleet_trip_events_handover_driver.sql
-- ---------------------------------------------------------------------------
-- WHAT:    add `handoverToDriverId` to `fleet_trip_events` — السائق المستلِم
--          في واقعة تسليم العهدة (handover) بين سائقين أثناء الرحلة.
--
-- WHY:     شريحة 3 (عهدة تبديل السائق). نوع الواقعة `handover` مهيّأ منذ
--          شريحة 1، لكن لتوثيق «مَن استلم العهدة» يلزم مرجع للسائق المستلِم
--          على الواقعة نفسها (سجل/تدقيق). إعادة إسناد أمر التوزيع للسائق
--          الجديد تتمّ ذرّيًا مع تسجيل الواقعة في الـendpoint.
--
-- DESIGN:
--   - عمود INTEGER اختياري (ADD COLUMN IF NOT EXISTS — توسّعي/آمن).
--   - مرجع ليّن لـfleet_drivers (يُتحقَّق في التطبيق: الـendpoint يتأكّد أنّ
--     المستلِم سائق صالح في الشركة + أهليّته)، اتساقًا مع
--     `recordedByAssignmentId`/`createdBy` (مراجع ليّنة بلا FK صلب).
--
-- SAFETY:  عمود اختياري إضافي، لا مساس بالدفتر، الجدول معزول إيجاريًا،
--          توسّعي بحت (الصفوف الحالية = NULL). الإذن: المالك اعتمد شريحة 3
--          (العهدة) كهجرة صغيرة (2026-06-29).
--
-- @rollback:
--   ALTER TABLE fleet_trip_events DROP COLUMN IF EXISTS "handoverToDriverId";
-- ===========================================================================

BEGIN;

ALTER TABLE fleet_trip_events
  ADD COLUMN IF NOT EXISTS "handoverToDriverId" INTEGER;

COMMIT;
