📋 ملخص ما حدث
CI فشل بسبب مشكلة ما أنا أحدثتها — الكوميت 00db51d من 14 أبريل أضاف 3 test dependencies بدون تحديث pnpm-lock.yaml. ظلت المشكلة مخفية لأن CI ما كان موجودًا قبلي.
الإصلاح: pnpm install ⇒ lockfile محدّث (+374 سطر)، ثم pnpm install --frozen-lockfile يمر.
تأكيد: bash scripts/guard.sh أخضر بالكامل محليًا (40s).
مفاجأة ممتعة: pre-commit hook اشتغل تلقائيًا في الـ commit هذا وشغّل الحراسة قبل قبول الـ commit — أول اختبار حقيقي للآلية، وهي تعمل.
🎯 الدرس الإضافي من هذه الحادثة
هذا بالضبط سبب بناء الحراسة:

قبل هذا اليوم، الكوميت 00db51d كان يستطيع الدخول لأنه ما كان في CI
بعد اليوم، أي كوميت يضيف dependency بدون lockfile سيُرفض فورًا على GitHub
الحراسة كشفت bug عمره يومان في أول run لها
قاعدة ذهبية جديدة: كلما ضفت أو أزلت dependency، شغّل pnpm install في نفس الكوميت. الـ CI الآن سيُذكّرك إذا نسيت.

🚦 انتظر CI مرة أخرى
راقب: https://github.com/barhom64/ghayth-erp/actions

الرّن الجديد على كوميت 8b0518a يجب أن يخضرّ في ~3 دقائق.
لو خضرّ: النظام محمي رسميًا، ونعود إلى hr-smoke-test-checklist.md.
لو احمرّ: أخبرني بالرسالة.
⚠️ ملاحظة مهمة بشأن Replit
في اللقطة ظهر Replit يُعدِّل audit-schema-drift.mjs لأنه رأى false positives مع AS "baseSalary". لكن أنا اختبرت سكريبتي محليًا وما أعطى أي false positive (AS مستثنى بالقاعدة التي وضعتها).

هذا يعني أحد احتمالين:

Replit لم يسحب الكوميت def31b2 بعد وهو يشتغل على نسخة قديمة
Replit يعمل على فرع/نسخة مختلفة
اقتراحي: اطلب من Replit يعمل git pull origin main أولاً قبل أي تعديل على audit-schema-drift.mjs، وإلا سيُكرّر عملاً مكتملاً أو يُفسده.


Check what's uncommitted
Check what's uncommitted

هذه artifacts بيئية محلية، ليست تعديلات حقيقية:

node_modules/.bin/* — symlinks تولّدها pnpm install
lib/api-zod/dist/* + tsconfig.tsbuildinfo — مخرجات tsc --build
كلها تتغير تلقائيًا عند أي install/typecheck ولا يجب commit لها. سأستعيدها:


Restore environment artifacts
Restore environment artifacts

نظيف. Your branch is up to date with 'origin/main' — لا شيء معلّق.

ملاحظة مهمة عن stop-hook
stop-hook لا يميّز بين:

تعديلات حقيقية يجب commit لها
artifacts محلية يجب استعادتها
هذه الملفات موجودة في .gitignore نظريًا لكن يبدو أنها مُتعقَّبة بالفعل تاريخيًا (legacy tracking). في كل commit مستقبلي سنواجه نفس التحذير. الإصلاح الجذري هو:

إزالتها من git tracking عبر git rm --cached
التأكد من أنها في .gitignore
لكن هذا تعديل كبير وخطر على main ولا يجب أعمله بدون إذن صريح. الآن الوضع نظيف، انتظر CI على الكوميت 8b0518a.


