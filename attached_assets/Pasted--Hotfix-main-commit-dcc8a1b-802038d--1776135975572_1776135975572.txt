## 🔥 Hotfix مدفوع إلى main — commit `dcc8a1b`

### ما تم دفعه

**قرار ذكي:** لم أرفع `802038d` كما هو (يحتوي 13 ملف بعضها غير ضروري للـ hotfix). بدلاً عنه:
- checkout'ت `main` محلياً، طبّقت الإصلاحات المحدّدة على ما هو مكسور على main فعلياً، كوّنت commit جديد وخفيف
- النتيجة: commit hotfix بـ 6 ملفات فقط (الضروري حصرياً)

**الملفات الستة في `dcc8a1b`:**

| ملف | التعديل |
|---|---|
| `src/lib/roleGuards.ts` (جديد) | Helper مشترك واحد يُصدّر `assertRole` — single source of truth |
| `src/routes/finance.ts` | import `assertRole` + حذف الـ orphaned `requireRole` |
| `src/routes/finance-zatca.ts` | import `assertRole` + إضافة TypedError imports الناقصة + حذف orphaned |
| `src/routes/finance-algorithms.ts` | إضافة `ValidationError/NotFoundError/ConflictError` المفقودين |
| `src/routes/finance-reports.ts` | إضافة `NotFoundError` المفقود |
| `src/routes/fleet.ts` | Test 11 refuse-list في PATCH /trips + PATCH /maintenance |

### ما لم يُدفع
- C.4 Property / C.5 Projects / C.6 Legal / C.7 Finance الكامل / C.8 Warehouse → بقي على فرع العمل كما هو
- docs/verification/fleet.md → لم يتم ضمّه في هذا الـ hotfix (يمكن دفعه كـ commit منفصل لاحقاً)

### أدلة التحقق من الـ hotfix

قبل الدمج وبعده على بيئة sandbox المحلية:

| اختبار | قبل | بعد |
|---|---|---|
| `npx tsc --noEmit` | **74 errors** (finance files) | **0 errors** ✅ |
| `pnpm run build` (esbuild) | ✅ (esbuild متسامح) | ✅ |
| Bundle grep `function assertRole` | **0 definitions** | **1 definition** (السطر 104143) ✅ |
| Bundle grep `assertRole(` callsites | **37 (broken)** | **37 (resolved)** ✅ |
| API boot `/api/healthz` | — | **200 OK** ✅ |
| API logs: `ReferenceError` | كان سيظهر عند أول finance request | **لا شيء** ✅ |

### حالة Test 11 — smoke test محدود
لم أتمكن من تشغيل Test 11 الفعلي في sandbox لأن الـ schema المحلي فارغ (نفس عائق Phase 2 الذي ناقشناه). الحجج الساكنة مرصوفة:
- الكود في `fleet.ts` سطر 1599 (trips) + سطر 1733 (maintenance) يرفض `completed`/`cancelled` صراحةً
- `ConflictError` مع `fix` يتضمن `POST /trips/:id/complete` / `POST /maintenance/:id/complete`
- Bundle يحتوي السلسلة: تأكّدت بأن النص العربي موجود في dist/index.mjs

### حالة Finance smoke test
حاولت POST /api/finance/invoices بـ JWT مُولَّد محلياً:
- `authMiddleware` يقبل الـ JWT (توقيع سليم)
- ثم يفشل في `buildScope` بـ `relation "employee_assignments" does not exist` (DB فارغ على sandbox)
- **المهم:** الفشل في `buildScope`، **لا ReferenceError**. يعني `assertRole` لم يصل إليه لأن auth توقف أولاً
- الحجة النهائية هي bundle grep: الـ definition موجود، الـ callsites مرتبطة

## 📋 المطلوب من المبرمج على Replit

**Smoke Test A — Test 11 re-run:**
```bash
# أنشئ رحلة in_progress جديدة (بنفس طريقة Part 3 من docs/verification/fleet.md)
curl -sS -X PATCH "$BASE_URL/api/fleet/trips/$TRIP_ID" \
  -H "$AUTH" -H "Content-Type: application/json" \
  -d '{"status":"completed"}'
```
**متوقع:** 409 CONFLICT، field=status، fix يحتوي `POST /trips/:id/complete`.
**قبل الـ hotfix:** 200 (bypass).

**Smoke Test B — Finance role gate:**
```bash
curl -sS -X POST "$BASE_URL/api/finance/invoices" \
  -H "$AUTH" -H "Content-Type: application/json" \
  -d '{"clientId":1,"subtotal":100,"branchId":1}'
```
**متوقع:** استجابة منظمة (إما 201 إذا البيانات صحيحة، أو 422/403 إذا نقص حقل أو دور — بـ `{error, code, field}`).
**قبل الـ hotfix:** 500 + stack trace يحتوي `ReferenceError: assertRole is not defined`.

**Smoke Test C — ZATCA route (نفس المسار):**
```bash
curl -sS -X POST "$BASE_URL/api/finance/zatca/encode-qr" \
  -H "$AUTH" -H "Content-Type: application/json" \
  -d '{"sellerName":"test","vatNumber":"123"}'
```
متوقع: استجابة منظمة، **لا** ReferenceError.

## 📌 حالة الفروع الآن

```
origin/main                              origin/claude/review-complete-task-eyEgc
  dcc8a1b (hotfix) ← HEAD                  802038d (full fix + C.4-C.8)
  9b0f5aa (auto-sync)                      828c630 (docs fleet.md)
  aecd27f (Fleet C.3 + finance.ts)         dbc67c3 (finance commas)
  ...                                      59c4efa (Warehouse)
                                           a1e54be (Finance full)
                                           17dfd40 (Legal)
                                           78038b4 (Projects)
                                           cbad4bb (Property)
                                           5ff8d4f (Fleet)
                                           a62e50e (shared ancestor)
```

- **main** نظيف compile‑wise + خالي من assertRole regression + يحتوي Test 11 refuse-list
- **work branch** لا يزال مصدر C.4-C.8 (بعضها يحتوي أخطاء TypeScript سابقة في finance — يجب re-audit قبل أي دمج)

## الخطوة التالية

**انتظر رد المبرمج** على Smoke Tests A/B/C. إذا:
- جميعها ✅ → **Fleet = 15/15 Verified رسمياً** + production-safe finance → ننتقل إلى Property (C.4)
- أي فشل → توقف وأرسل التقرير

لا أبدأ Property قبل تأكيد المبرمج على الـ hotfix.