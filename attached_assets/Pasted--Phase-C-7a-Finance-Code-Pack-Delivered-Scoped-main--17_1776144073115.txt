## Phase C.7a Finance — Code + Pack Delivered (Scoped)

### الحالة الآن

```
main                                           claude/phase-c7-finance
  0542b57 (Legal C.6 verified)                   95fd377 (gitignore) ← HEAD
  d3c0987                                        5fa0bea (Finance pack docs)
  3fdde57                                        1fc688a (Finance big-3 code)
  515192a                                        0542b57
  ...                                            ...
```

### قرار الـ scope

الكود الأصلي لـ Phase C.7 اللي كتبته حاول تحويل **14 ملف Finance** دفعة واحدة. الـ regex كان فيه bug (الـ `[^}]*?` لا يطابق أقواس متداخلة) فكسر 11 ملف. الـ hotfix `dcc8a1b` أنقذ 4 منهم (finance.ts, finance-zatca, finance-algorithms, finance-reports). الباقي **لم يلمس main أبداً**.

**قرار هذه الجولة (C.7a): تركيز على 3 ملفات فقط**:
- `finance-invoices.ts` (+366/-140) — invoice lifecycle
- `finance-journal.ts` (+253/-110) — JE + expense + vouchers + budget guardrails
- `finance-purchase.ts` (+106/-78) — PR + PO

**الـ 7 ملفات الباقية مؤجّلة لـ C.7b** (بعد C.8 Warehouse): accounts, budget, collection, custodies, hardening, recurring, vendors — كلها simple CRUD بدون lifecycle state machines، أولوية أدنى.

### Branch `claude/phase-c7-finance`

**Commit 1 — `1fc688a`:** الكود للـ 3 ملفات، مستخرج من work branch commit 802038d (اللي تحقّقت منه تايب‑تشيك سابقاً). tsc: 0 errors، esbuild: clean bundle.

**Commit 2 — `5fa0bea`:** `docs/verification/finance.md` — 1073 سطر، 14 سيناريو

### الـ 14 سيناريو

| # | Test | Expected |
|---|---|---|
| 1 | POST /invoices missing clientId | 422 |
| 2 | POST /invoices missing branchId (scope fallback) | 422 OR 201 N/A |
| 3 | POST /invoices line unitPrice ≤ 0 | 422 |
| 4 | POST /invoices happy path | 201 + invoice.created + JE |
| 5 | **PATCH direct status=sent** | **409** (defence-in-depth) |
| 6 | **PATCH direct status=paid** | **409** (defence-in-depth) |
| 7 | /payment amount > remaining | 422 |
| 8 | /payment on paid invoice | 409 |
| 9 | DELETE paidAmount > 0 | 409 |
| 10 | POST /expenses missing accountCode | 422 |
| 11 | **/expenses budget > 110%** | **409 + meta.utilization + meta.status** 🎯 |
| 12 | DELETE /accounts linked entries | 409 + meta.linkedEntries |
| 13 | POST /purchase-requests empty items | 422 |
| 14 | Approval reject without notes | 422 |

### Test 11 هو الأهم

اختبار الـ budget guardrail الثلاثي tiers في request واحد:
- `> 110%` → `ConflictError` (hard reject) — **hotfix test**
- `100-110%` → `ForbiddenError` unless GM/owner
- `80-99%` → `ForbiddenError` unless CFO/GM/owner

كلها تحمل `meta.utilization` + `meta.status`. هذا هو الـ regression-test للنمط الموحد.

### Static Review verdict

**13/14 ✅ + 1 N/A** متوقع. لا Fleet-Test-11-style gap — PATCH refuse-list لـ `sent/paid/partial` مبنيّ من اليوم الأول في `finance-invoices.ts`.

### Schema drift watchlist

الـ pack يشمل §1.2 تحذيرات بخصوص أعمدة ZATCA (`isTaxLinked`, `invoiceTypeCode`, إلخ) و أعمدة `journal_entries` المتراكمة (`operationType`, `sourceType`, إلخ). لو live DB أقدم من migrations 023/052، الـ INSERT يحتاج تقليم (نفس نمط Property building fix).

### Build state

- tsc --noEmit: 0 errors ✅
- pnpm run build: clean 7.0mb bundle ✅
- Branch pushed to `origin/claude/phase-c7-finance`
- Working tree: نظيفة ✅

main لا يزال على `0542b57`. Finance big-3 على فرع منفصل بانتظار التأكيد. بعد verification الناجح، ننتقل إلى **Phase C.8 Warehouse** (الأخيرة)، ثم نرجع لـ C.7b إذا أردت.